#pragma once

#include "plugins/PluginInterface.h"

namespace workpane::plugins::ai::translations {

class AiCatalog final {
  public:
    static TranslationEntries english() {
        return {
            {QStringLiteral("ai.tool-title.read-file"), QStringLiteral("Read File")}, {QStringLiteral("ai.template.application-name"), QStringLiteral("Application builder")}, {QStringLiteral("ai.template.application-description"), QStringLiteral("Builds and maintains an application, from its layout to its packaging.")}, {QStringLiteral("ai.template.application-body"), QStringLiteral("You are {{AGENT_NAME}}. {{AGENT_DESCRIPTION}}\n\n{{SYSTEM_PROMPT_DATA}}\n\n# The assignment\n\n{{TASK_TITLE}}\n{{TASK_DESCRIPTION}}\n\nWorking directory: {{TASK_WORKDIR}}\nIssue: {{TASK_ISSUE_URL}}\n\n# What this run really has\n\nModel {{MODEL}} declaring {{MODEL_TRAITS}}, a context window of {{CONTEXT_WINDOW}} and an answer budget of {{OUTPUT_BUDGET}}.\nTools: {{TOOLS}}\n{{VISION}}\n{{SEARCH}}\n{{SPEECH}}\n{{SERVERS}}\n{{SKILLS}}\n{{CONTEXT_FILES}}\n\n# How you build an application\n\nRead the project before you change it. Find how it is built, how it is run and how it is tested, and\nfollow those paths rather than inventing your own. The conventions already in the code win over your\npreferences.\n\nLayout. Keep one responsibility per module and let the directory structure say what each part owns.\nDependencies point one way: the domain does not depend on the interface or on storage. A name says\nwhat a thing is, so a well-named module needs no comment explaining its existence.\n\nDependencies and build. Prefer what the standard library and the existing dependencies already give\nyou. Add a dependency only when it removes real work, and pin it the way the project pins the others.\nEvery change must leave the build green with its warnings still treated as errors.\n\nState. Make the invalid state unrepresentable where the language lets you. Keep mutable state in as\nfew places as possible, and make ownership explicit rather than implied. Anything shared across\nthreads is either immutable or guarded, never merely hoped to be safe.\n\nError handling. Every failure path returns something the caller can act on, carrying a stable code\nand a message. Never swallow a failure, never turn one into an empty value, and never leave state\nhalf-mutated when a step fails: either the whole operation lands or none of it does.\n\nAsynchrony. Nothing that can wait on storage, a process, the network or another thread runs on the\nthread that draws. Cancellation is explicit and idempotent. A callback bound to an object that can be\ndestroyed is guarded, so a completion never reaches something that is already gone.\n\nPersistence. Validate everything read back before you trust it. A stored value the code cannot use is\na declared default or an explicit failure, never a silent guess. A schema change is additive and\ncarries whatever the reader already stored.\n\nTests. Every behaviour you add carries cases for the success path, the failure path and the boundary.\nA test asserts what the product guarantees rather than what a platform happens to do. Run the suite\nand read the output, and never report a change as done while a case is red.\n\nPackaging. Know how the product is packaged and confirm your change survives it. A change that builds\nbut cannot ship is not finished.\n\nReport what you did, what you verified and what you deliberately left out.")}, {QStringLiteral("ai.template.code-review-name"), QStringLiteral("Code reviewer")}, {QStringLiteral("ai.template.code-review-description"), QStringLiteral("Finds real defects, from memory faults and leaks to races and unbounded growth.")}, {QStringLiteral("ai.template.code-review-body"), QStringLiteral("You are {{AGENT_NAME}}. {{AGENT_DESCRIPTION}}\n\n{{SYSTEM_PROMPT_DATA}}\n\n# The assignment\n\n{{TASK_TITLE}}\n{{TASK_DESCRIPTION}}\n\nWorking directory: {{TASK_WORKDIR}}\nIssue: {{TASK_ISSUE_URL}}\n\n# What this run really has\n\nModel {{MODEL}} declaring {{MODEL_TRAITS}}, a context window of {{CONTEXT_WINDOW}} and an answer budget of {{OUTPUT_BUDGET}}.\nTools: {{TOOLS}}\n{{VISION}}\n{{SEARCH}}\n{{SPEECH}}\n{{SERVERS}}\n{{SKILLS}}\n{{CONTEXT_FILES}}\n\n# How you review code\n\nYou look for defects that would really bite, and you report them with the input and the state that\nproduce them. Style is not a finding unless the project declares it. A change you cannot justify as\nfixing something is not a change.\n\nCorrectness. Check every boundary: the empty collection, the single element, the last index, the\nvalue that is exactly the limit and the one past it. Check integer overflow and the truncation of a\nwider type into a narrower one. Check every comparison of a signed value with an unsigned one.\n\nMemory. Look for a read or a write past the end of a buffer, for a use after free, for a pointer into\na container that is about to grow, for a reference to a temporary that dies at the end of the\nexpression, and for an iterator kept across a mutation of what it iterates.\n\nOwnership and lifetime. Find who owns each allocation and confirm it is released on every path,\nincluding the early returns and the failure paths. Look for a raw owning pointer where a smart one\nbelongs, for a double free and for an object that outlives something it holds a reference to.\n\nLeaks. Look for an allocation that no path releases, a handle or a descriptor never closed, a\nsubscription or a connection never disconnected, and a container that only ever grows.\n\nConcurrency. Find shared mutable state reached from more than one thread, and confirm every access is\nunder the same lock. Look for a value read under a lock but written outside it, for a condition\nsignalled without the state having been changed under that lock, for a lost wakeup, and for two locks\ntaken in different orders on different paths. Check that a callback delivered on another thread\ncannot reach an object that is already being destroyed.\n\nError paths. Confirm every failure is checked, that no result is silently dropped, and that a\nmulti-step change either lands whole or rolls back. Look for a failure turned into an empty value.\n\nResource bounds. Find anything that grows from something the caller decides: a recursion whose depth\ncomes from input, a read without a bound, an allocation from a length field.\n\nTests. Say plainly which of the cases you found is not covered, and what a case that catches it would\nhave to do.\n\nReport each finding as the defect, the concrete input or interleaving that triggers it, and the\nsmallest fix. Rank them by how much they cost when they happen.")}, {QStringLiteral("ai.template.security-name"), QStringLiteral("Security reviewer")}, {QStringLiteral("ai.template.security-description"), QStringLiteral("Reviews the application it is running on against attackers, intruders and holes.")}, {QStringLiteral("ai.template.security-body"), QStringLiteral("You are {{AGENT_NAME}}. {{AGENT_DESCRIPTION}}\n\n{{SYSTEM_PROMPT_DATA}}\n\n# The assignment\n\n{{TASK_TITLE}}\n{{TASK_DESCRIPTION}}\n\nWorking directory: {{TASK_WORKDIR}}\nIssue: {{TASK_ISSUE_URL}}\n\n# What this run really has\n\nModel {{MODEL}} declaring {{MODEL_TRAITS}}, a context window of {{CONTEXT_WINDOW}} and an answer budget of {{OUTPUT_BUDGET}}.\nTools: {{TOOLS}}\n{{VISION}}\n{{SEARCH}}\n{{SPEECH}}\n{{SERVERS}}\n{{SKILLS}}\n{{CONTEXT_FILES}}\n\n# How you review security\n\nYou review the application in the working directory for the ways a real attacker would get in. You\nreport what you find. You never exploit a live system, never exfiltrate anything and never weaken a\nprotection to prove a point.\n\nStart by mapping the attack surface: every place where data crosses a boundary. Network endpoints,\ncommand line arguments, environment variables, files the product reads, inter-process messages,\nuploaded content, the database and anything a plugin or an extension may contribute.\n\nInput. Every value that comes from outside is untrusted until it is validated against what it is\nallowed to be. Look for length, type, range and encoding checks, and for the absence of them. Reject\nrather than sanitise where you can, because a sanitiser you can defeat is worse than a refusal.\n\nInjection. Check every query built by concatenation, every command built from a string, every path\nbuilt from user input, every template rendered with unescaped content and every deserialisation of\ndata the product did not write. A parameterised query and an argument vector are the answers.\n\nPaths. Check that a path is judged by where it really lands rather than by how it was written, so a\ntraversal and a symbolic link that leaves the allowed root are both refused. Check that the deepest\nexisting ancestor is resolved before the containment test.\n\nAuthentication and authorisation. Look for the check that is missing rather than the one that is\nthere. Every privileged action verifies who is asking on the server side, and never on the strength\nof something the client sent. Session identifiers are unpredictable, rotated on privilege change and\ninvalidated on the way out.\n\nSecrets. Find every credential in the tree, in the history, in configuration, in logs and in error\nmessages. A secret must never be logged, never travel in a URL and never be compared with an\nearly-returning comparison. Check what the process passes to the children it starts.\n\nTransport and storage. Verify certificates rather than accepting any. Prefer authenticated encryption\nand a published key-derivation function over anything invented locally. Check what the product writes\nto disk and with what permissions.\n\nResource limits. Every buffer that grows from something an outsider sends must be bounded before the\nbytes arrive, not after. Look for unbounded recursion whose depth an input decides, for reads without\na deadline, for connection counts without a cap and for anything that allocates by a length field.\n\nDependencies. Check what is pinned and what floats, and what the declared versions are known to carry.\n\nReport each finding with where it is, what an attacker would do with it, how serious it is and the\nsmallest change that closes it. Say plainly what you could not check and why.")}, {QStringLiteral("ai.template.website-name"), QStringLiteral("Website builder")}, {QStringLiteral("ai.template.website-description"), QStringLiteral("Builds and maintains a website, from its structure to how it is verified.")}, {QStringLiteral("ai.template.website-body"), QStringLiteral("You are {{AGENT_NAME}}. {{AGENT_DESCRIPTION}}\n\n{{SYSTEM_PROMPT_DATA}}\n\n# The assignment\n\n{{TASK_TITLE}}\n{{TASK_DESCRIPTION}}\n\nWorking directory: {{TASK_WORKDIR}}\nIssue: {{TASK_ISSUE_URL}}\n\n# What this run really has\n\nModel {{MODEL}} declaring {{MODEL_TRAITS}}, a context window of {{CONTEXT_WINDOW}} and an answer budget of {{OUTPUT_BUDGET}}.\nTools: {{TOOLS}}\n{{VISION}}\n{{SEARCH}}\n{{SPEECH}}\n{{SERVERS}}\n{{SKILLS}}\n{{CONTEXT_FILES}}\n\n# How you build a site\n\nRead what is already there before you write anything. The conventions of the project win over your own\npreferences, so follow the structure, the naming and the formatting the existing files establish.\n\nStructure. Keep one page per document, put shared markup in the partials or components the project\nalready uses, and never duplicate a block you could include. Separate content, presentation and\nbehaviour: markup describes what the page means, styling decides how it looks, and scripting is only\nfor what genuinely needs to run.\n\nMarkup. Write valid semantic HTML. Use headings in order and never skip a level, use lists for lists,\nbuttons for actions and links for navigation. Every image carries alternative text that says what the\nimage is for, and an image that is decoration carries an empty one. Every form control has a label.\nThe document declares its language and its character set.\n\nStyling. Prefer the project's existing system, whether that is plain CSS, a preprocessor or a utility\nframework. Use relative units so text scales with the reader's settings. Layout with flexbox and grid\nrather than with floats or absolute positioning. Define colour, spacing and typography as tokens and\nreference them, so a change lands in one place.\n\nResponsiveness. Design for the narrow viewport first and add what the wider one allows. Nothing may\nscroll the page sideways: wide tables, code blocks and diagrams scroll inside their own container.\nTest the layout at a small phone, a tablet and a wide desktop before you call it done.\n\nAccessibility. Every interactive element is reachable and operable from the keyboard, and the focus\nring stays visible. Colour never carries meaning on its own. Contrast meets the published ratio for\nbody text and for large text. Respect the reduced-motion preference.\n\nPerformance. Serve images in a modern format at the size they are displayed, give them explicit\ndimensions so the page does not shift while loading, and load below-the-fold ones lazily. Keep the\ncritical path small, defer scripts that are not needed to render, and do not add a dependency for\nsomething a few lines would do.\n\nVerification. After every change, read the files you wrote back. If the project has a build, run it.\nIf it has a linter, a formatter or tests, run them and fix what they report. When a server is\navailable, open the page and confirm what you changed really renders. Never report work as finished\non the strength of having written it.\n\nReport what you did, what you verified and what you deliberately left out.")}, {QStringLiteral("ai.tool-activity.read-file"), QStringLiteral("Reading %1")}, {QStringLiteral("ai.tool-title.write-file"), QStringLiteral("Write File")}, {QStringLiteral("ai.tool-activity.write-file"), QStringLiteral("Writing %1")}, {QStringLiteral("ai.tool-title.edit-file"), QStringLiteral("Edit File")}, {QStringLiteral("ai.tool-activity.edit-file"), QStringLiteral("Editing %1")}, {QStringLiteral("ai.tool-title.read-image"), QStringLiteral("View Image")}, {QStringLiteral("ai.tool-activity.read-image"), QStringLiteral("Viewing %1")}, {QStringLiteral("ai.tool-title.list-directory"), QStringLiteral("List Directory")}, {QStringLiteral("ai.tool-activity.list-directory"), QStringLiteral("Listing %1")}, {QStringLiteral("ai.tool-title.create-directory"), QStringLiteral("Create Directory")}, {QStringLiteral("ai.tool-activity.create-directory"), QStringLiteral("Creating %1")}, {QStringLiteral("ai.tool-title.move-path"), QStringLiteral("Move")}, {QStringLiteral("ai.tool-activity.move-path"), QStringLiteral("Moving %1")}, {QStringLiteral("ai.tool-title.copy-file"), QStringLiteral("Copy")}, {QStringLiteral("ai.tool-activity.copy-file"), QStringLiteral("Copying %1")}, {QStringLiteral("ai.tool-title.remove-path"), QStringLiteral("Remove")}, {QStringLiteral("ai.tool-activity.remove-path"), QStringLiteral("Removing %1")}, {QStringLiteral("ai.tool-title.describe-path"), QStringLiteral("Describe Path")}, {QStringLiteral("ai.tool-activity.describe-path"), QStringLiteral("Describing %1")}, {QStringLiteral("ai.tool-title.search-files"), QStringLiteral("Search Files")}, {QStringLiteral("ai.tool-activity.search-files"), QStringLiteral("Searching for: %1")}, {QStringLiteral("ai.tool-title.run-command"), QStringLiteral("Run Command")}, {QStringLiteral("ai.tool-activity.run-command"), QStringLiteral("Running: %1")}, {QStringLiteral("ai.tool-title.fetch-url"), QStringLiteral("Fetch Page")}, {QStringLiteral("ai.tool-activity.fetch-url"), QStringLiteral("Fetching %1")}, {QStringLiteral("ai.tool-title.web-search"), QStringLiteral("Web Search")}, {QStringLiteral("ai.tool-activity.web-search"), QStringLiteral("Searching for: %1")}, {QStringLiteral("ai.tool-title.generate-image"), QStringLiteral("Generate Image")}, {QStringLiteral("ai.tool-activity.generate-image"), QStringLiteral("Drawing: %1")}, {QStringLiteral("ai.tool-title.generate-speech"), QStringLiteral("Generate Speech")}, {QStringLiteral("ai.tool-activity.generate-speech"), QStringLiteral("Speaking: %1")}, {QStringLiteral("ai.tool-title.list-voices"), QStringLiteral("List Voices")}, {QStringLiteral("ai.tool-title.list-skills"), QStringLiteral("List Skills")}, {QStringLiteral("ai.tool-title.search-skills"), QStringLiteral("Search Skills")}, {QStringLiteral("ai.tool-activity.search-skills"), QStringLiteral("Searching for: %1")}, {QStringLiteral("ai.tool-title.read-skill"), QStringLiteral("Read Skill")}, {QStringLiteral("ai.tool-activity.read-skill"), QStringLiteral("Reading %1")}, {QStringLiteral("ai.tool-title.describe-task"), QStringLiteral("Read Task")}, {QStringLiteral("ai.tool-title.list-mcp-resources"), QStringLiteral("List Resources")}, {QStringLiteral("ai.tool-title.read-mcp-resource"), QStringLiteral("Read Resource")}, {QStringLiteral("ai.tool-activity.read-mcp-resource"), QStringLiteral("Reading %1")}, {QStringLiteral("ai.tool-title.list-mcp-prompts"), QStringLiteral("List Prompts")}, {QStringLiteral("ai.tool-title.read-mcp-prompt"), QStringLiteral("Read Prompt")}, {QStringLiteral("ai.tool-activity.read-mcp-prompt"), QStringLiteral("Reading %1")}, {QStringLiteral("ai.plugin.title"), QStringLiteral("AI")}, {QStringLiteral("ai.navigation.tasks"), QStringLiteral("Tasks")}, {QStringLiteral("ai.tasks.title"), QStringLiteral("AI Tasks")}, {QStringLiteral("ai.error.title"), QStringLiteral("AI Tasks")}, {QStringLiteral("ai.task.tab-general"), QStringLiteral("General")}, {QStringLiteral("ai.task.tab-prompt"), QStringLiteral("Prompt")}, {QStringLiteral("ai.task.tab-chat"), QStringLiteral("Chat")}, {QStringLiteral("ai.task.chat"), QStringLiteral("Open the conversation")}, {QStringLiteral("ai.error.stop"), QStringLiteral("The run could not be stopped")}, {QStringLiteral("ai.task.tab-executions"), QStringLiteral("Executions")}, {QStringLiteral("ai.task.tab-logs"), QStringLiteral("Logs")}, {QStringLiteral("ai.task.tab-output"), QStringLiteral("Output")}, {QStringLiteral("ai.task.title"), QStringLiteral("Title")}, {QStringLiteral("ai.task.description"), QStringLiteral("Description")}, {QStringLiteral("ai.task.prompt"), QStringLiteral("Prompt")}, {QStringLiteral("ai.task.prompt-placeholder"), QStringLiteral("What this task asks the model to do")}, {QStringLiteral("ai.task.add"), QStringLiteral("New Task")}, {QStringLiteral("ai.task.edit"), QStringLiteral("Edit")}, {QStringLiteral("ai.task.info"), QStringLiteral("Task Information")}, {QStringLiteral("ai.task.start"), QStringLiteral("Run")}, {QStringLiteral("ai.task.stop"), QStringLiteral("Stop")}, {QStringLiteral("ai.task.remove"), QStringLiteral("Remove Task")}, {QStringLiteral("ai.task.remove-message"), QStringLiteral("Remove this task permanently?")}, {QStringLiteral("ai.task.last-error"), QStringLiteral("Last error: %1")}, {QStringLiteral("ai.task.scheduled"), QStringLiteral("Next run %1")}, {QStringLiteral("ai.task.schedule"), QStringLiteral("Schedule")}, {QStringLiteral("ai.task.schedule-remove"), QStringLiteral("Stop Schedule")}, {QStringLiteral("ai.task.schedule-remove-message"), QStringLiteral("Stop the schedule of this task and remove it?")}, {QStringLiteral("ai.task.open-workdir"), QStringLiteral("Open the working directory in the Code Editor")}, {QStringLiteral("ai.task.serve-workdir"), QStringLiteral("Serve the working directory as a Web Server")}, {QStringLiteral("ai.badge.idle"), QStringLiteral("Idle")}, {QStringLiteral("ai.badge.scheduled"), QStringLiteral("Scheduled")}, {QStringLiteral("ai.badge.stopped"), QStringLiteral("Stopped")}, {QStringLiteral("ai.badge.queued"), QStringLiteral("Queued")}, {QStringLiteral("ai.badge.running"), QStringLiteral("Running")}, {QStringLiteral("ai.badge.succeeded"), QStringLiteral("Succeeded")}, {QStringLiteral("ai.badge.failed"), QStringLiteral("Failed")}, {QStringLiteral("ai.badge.cancelled"), QStringLiteral("Cancelled")}, {QStringLiteral("ai.status.running"), QStringLiteral("Running")}, {QStringLiteral("ai.status.succeeded"), QStringLiteral("Succeeded")}, {QStringLiteral("ai.status.failed"), QStringLiteral("Failed")}, {QStringLiteral("ai.status.cancelled"), QStringLiteral("Cancelled")}, {QStringLiteral("ai.execution.started"), QStringLiteral("Started")}, {QStringLiteral("ai.execution.status"), QStringLiteral("Status")}, {QStringLiteral("ai.execution.tokens"), QStringLiteral("Tokens")}, {QStringLiteral("ai.execution.cost"), QStringLiteral("Cost")}, {QStringLiteral("ai.execution.token-usage"), QStringLiteral("%1 in, %2 out")}, {QStringLiteral("ai.execution.finish-reason"), QStringLiteral("Finish reason")}, {QStringLiteral("ai.execution.error"), QStringLiteral("Error")}, {QStringLiteral("ai.execution.count"), QStringLiteral("%1 recorded executions")}, {QStringLiteral("ai.log.timestamp"), QStringLiteral("Time")}, {QStringLiteral("ai.log.level"), QStringLiteral("Level")}, {QStringLiteral("ai.log-level.debug"), QStringLiteral("Debug")}, {QStringLiteral("ai.log-level.info"), QStringLiteral("Info")}, {QStringLiteral("ai.log-level.warning"), QStringLiteral("Warning")}, {QStringLiteral("ai.log-level.error"), QStringLiteral("Error")}, {QStringLiteral("ai.settings.provider"), QStringLiteral("Provider")}, {QStringLiteral("ai.settings.model"), QStringLiteral("Model")}, {QStringLiteral("ai.settings.api-key"), QStringLiteral("API key")}, {QStringLiteral("ai.settings.api-key-placeholder"), QStringLiteral("Key or {env.NAME}")}, {QStringLiteral("ai.settings.chat-font-size"), QStringLiteral("Chat font size")}, {QStringLiteral("ai.settings.parallel-limit"), QStringLiteral("Parallel executions")}, {QStringLiteral("ai.settings.reveal-title"), QStringLiteral("Reveal API Key")}, {QStringLiteral("ai.settings.reveal-message"), QStringLiteral("Show the stored API key in plain text?")}, {QStringLiteral("ai.settings.reveal-detail"), QStringLiteral("Anyone able to see this screen will be able to read the key.")}, {QStringLiteral("ai.settings.reveal-action"), QStringLiteral("Reveal Key")}, {QStringLiteral("ai.provider.grok-cli"), QStringLiteral("Grok Build CLI")}, {QStringLiteral("ai.provider.claude-cli"), QStringLiteral("Claude Code CLI")}, {QStringLiteral("ai.provider.codex-cli"), QStringLiteral("Codex CLI")}, {QStringLiteral("ai.provider.antigravity-cli"), QStringLiteral("Google Antigravity CLI")}, {QStringLiteral("ai.provider.kimi-cli"), QStringLiteral("Kimi Code CLI")}, {QStringLiteral("ai.error.cli-provider-invalid"), QStringLiteral("This connection does not name a command line agent")}, {QStringLiteral("ai.error.cli-workdir-required"), QStringLiteral("A command line agent needs the working directory of the task")}, {QStringLiteral("ai.error.cli-program-missing"), QStringLiteral("The command line agent is not installed on this system")}, {QStringLiteral("ai.provider.elevenlabs"), QStringLiteral("ElevenLabs")}, {QStringLiteral("ai.provider.anthropic"), QStringLiteral("Anthropic")}, {QStringLiteral("ai.provider.openai"), QStringLiteral("OpenAI")}, {QStringLiteral("ai.provider.openrouter"), QStringLiteral("OpenRouter")}, {QStringLiteral("ai.provider.deepseek"), QStringLiteral("DeepSeek")}, {QStringLiteral("ai.provider.xai"), QStringLiteral("xAI Grok")}, {QStringLiteral("ai.provider.gemini"), QStringLiteral("Google Gemini")}, {QStringLiteral("ai.provider.moonshot"), QStringLiteral("Moonshot Kimi")}, {QStringLiteral("ai.provider.groq"), QStringLiteral("Groq")}, {QStringLiteral("ai.provider.mistral"), QStringLiteral("Mistral")}, {QStringLiteral("ai.provider.together"), QStringLiteral("Together AI")}, {QStringLiteral("ai.provider.fireworks"), QStringLiteral("Fireworks AI")}, {QStringLiteral("ai.provider.deepinfra"), QStringLiteral("DeepInfra")}, {QStringLiteral("ai.provider.perplexity"), QStringLiteral("Perplexity")}, {QStringLiteral("ai.provider.cerebras"), QStringLiteral("Cerebras")}, {QStringLiteral("ai.provider.sambanova"), QStringLiteral("SambaNova")}, {QStringLiteral("ai.provider.nvidia"), QStringLiteral("NVIDIA NIM")}, {QStringLiteral("ai.provider.ollama"), QStringLiteral("Ollama")}, {QStringLiteral("ai.provider.lm-studio"), QStringLiteral("LM Studio")}, {QStringLiteral("ai.parameter.max-output-tokens"), QStringLiteral("Maximum output tokens")}, {QStringLiteral("ai.parameter.temperature"), QStringLiteral("Temperature")}, {QStringLiteral("ai.parameter.top-p"), QStringLiteral("Top P")}, {QStringLiteral("ai.parameter.frequency-penalty"), QStringLiteral("Frequency penalty")}, {QStringLiteral("ai.parameter.presence-penalty"), QStringLiteral("Presence penalty")}, {QStringLiteral("ai.parameter.reasoning-effort"), QStringLiteral("Reasoning effort")}, {QStringLiteral("ai.effort.low"), QStringLiteral("Low")}, {QStringLiteral("ai.effort.medium"), QStringLiteral("Medium")}, {QStringLiteral("ai.effort.high"), QStringLiteral("High")}, {QStringLiteral("ai.column.todo"), QStringLiteral("To Do")}, {QStringLiteral("ai.column.doing"), QStringLiteral("Doing")}, {QStringLiteral("ai.column.blocked"), QStringLiteral("Blocked")}, {QStringLiteral("ai.column.review"), QStringLiteral("Review")}, {QStringLiteral("ai.column.done"), QStringLiteral("Done")}, {QStringLiteral("ai.workspace.add"), QStringLiteral("New Workspace")}, {QStringLiteral("ai.workspace.rename"), QStringLiteral("Rename Workspace")}, {QStringLiteral("ai.workspace.remove"), QStringLiteral("Remove Workspace")}, {QStringLiteral("ai.workspace.remove-message"), QStringLiteral("Remove this workspace and every task it contains?")}, {QStringLiteral("ai.workspace.name"), QStringLiteral("Workspace name")}, {QStringLiteral("ai.workspace.empty"), QStringLiteral("Create a workspace to organize your AI tasks")}, {QStringLiteral("ai.schedule.type"), QStringLiteral("Schedule type")}, {QStringLiteral("ai.schedule.none"), QStringLiteral("Not scheduled")}, {QStringLiteral("ai.schedule.once"), QStringLiteral("One time")}, {QStringLiteral("ai.schedule.interval"), QStringLiteral("Interval")}, {QStringLiteral("ai.schedule.cron"), QStringLiteral("POSIX cron")}, {QStringLiteral("ai.schedule.minutes"), QStringLiteral(" minutes")}, {QStringLiteral("ai.validation.title"), QStringLiteral("Enter a title for the task")}, {QStringLiteral("ai.validation.prompt"), QStringLiteral("Enter the prompt the model receives")}, {QStringLiteral("ai.validation.schedule-past"), QStringLiteral("The one-time schedule must be in the future")}, {QStringLiteral("ai.validation.cron"), QStringLiteral("The cron expression must use five portable POSIX fields")}, {QStringLiteral("ai.task.issue-url"), QStringLiteral("Issue address")}, {QStringLiteral("ai.task.issue-url-placeholder"), QStringLiteral("Address of the issue this task belongs to")}, {QStringLiteral("ai.validation.issue-url"), QStringLiteral("The issue address must be a valid HTTP or HTTPS address")}, {QStringLiteral("ai.tool.describe-task"), QStringLiteral("Read the task you are working on, with its title, description, issue address and working directory")}, {QStringLiteral("ai.log-kind.compacted"), QStringLiteral("Conversation compacted")}, {QStringLiteral("ai.log.compacted"), QStringLiteral("Dropped %1 older turns to fit the model context window")}, {QStringLiteral("ai.log.pruned"), QStringLiteral("Shortened %1 older tool results to fit the model context window")}, {QStringLiteral("ai.tool.list-skills"), QStringLiteral("List the skills available in this workspace with their descriptions")}, {QStringLiteral("ai.tool.search-skills"), QStringLiteral("Search the available skills by name and description")}, {QStringLiteral("ai.tool.read-skill"), QStringLiteral("Load the full instructions of one skill by name")}, {QStringLiteral("ai.tool.read-skill-file"), QStringLiteral("Read a file the skill carries beside its instructions, such as its reference or its README")}, {QStringLiteral("ai.tool-title.read-skill-file"), QStringLiteral("Read Skill File")}, {QStringLiteral("ai.tool-activity.read-skill-file"), QStringLiteral("Reading %1")}, {QStringLiteral("ai.skill.none"), QStringLiteral("This workspace declares no skills")}, {QStringLiteral("ai.skill.no-match"), QStringLiteral("No skill matches %1")}, {QStringLiteral("ai.skill.unknown"), QStringLiteral("There is no skill named %1")}, {QStringLiteral("ai.agent.context-file"), QStringLiteral("Instructions from %1:\n\n%2")}, {QStringLiteral("ai.agent.skills"), QStringLiteral("The following skills are available. Load one with the skill tool when it applies to the task.")}, {QStringLiteral("ai.agent.workdir"), QStringLiteral("Your working directory is %1. Every file path you use stays inside it, written relative to it or in full, and you cannot reach outside it.")}, {QStringLiteral("ai.agent.no-workdir"), QStringLiteral("This task declares no working directory, so the file tools are unavailable to you.")}, {QStringLiteral("ai.tool.generate-speech"), QStringLiteral("Turn text into speech and store it as an execution artifact")}, {QStringLiteral("ai.error.tool-speech-unconfigured"), QStringLiteral("Speech generation needs text and a configured speech provider")}, {QStringLiteral("ai.error.tool-speech-empty"), QStringLiteral("The speech provider returned no audio")}, {QStringLiteral("ai.settings.speech"), QStringLiteral("Speech provider")}, {QStringLiteral("ai.settings.speech-voice"), QStringLiteral("Voice")}, {QStringLiteral("ai.settings.speech-key"), QStringLiteral("API key")}, {QStringLiteral("ai.tool.generate-image"), QStringLiteral("Generate an image from a description and store it as an execution artifact")}, {QStringLiteral("ai.error.tool-image-unconfigured"), QStringLiteral("Image generation needs a prompt and a configured provider address")}, {QStringLiteral("ai.error.tool-image-empty"), QStringLiteral("The provider returned no image data")}, {QStringLiteral("ai.tool.fetch-url"), QStringLiteral("Fetch a web page and return its readable text")}, {QStringLiteral("ai.error.tool-url"), QStringLiteral("The address %1 is not a valid HTTP or HTTPS address")}, {QStringLiteral("ai.tool.read-file"), QStringLiteral("Read a text file inside the task working directory")}, {QStringLiteral("ai.tool.write-file"), QStringLiteral("Write a text file inside the task working directory")}, {QStringLiteral("ai.tool.edit-file"), QStringLiteral("Replace an exact passage of a text file inside the task working directory")}, {QStringLiteral("ai.tool.read-image"), QStringLiteral("Look at an image file inside the task working directory")}, {QStringLiteral("ai.error.tool-image-unsupported"), QStringLiteral("The model %1 does not accept images, so it cannot look at one")}, {QStringLiteral("ai.error.tool-image-format"), QStringLiteral("The file %1 is not a PNG, JPEG, WebP or GIF image")}, {QStringLiteral("ai.error.tool-image-size"), QStringLiteral("The image %1 is empty or larger than the tool accepts")}, {QStringLiteral("ai.error.tool-edit-absent"), QStringLiteral("The file %1 does not contain that exact text. Read it again and copy the passage exactly as it is written there.")}, {QStringLiteral("ai.error.tool-edit-ambiguous"), QStringLiteral("That text appears %1 times, so name a longer passage or ask to replace every occurrence")}, {QStringLiteral("ai.error.tool-not-text"), QStringLiteral("%1 is not UTF-8 text, so it cannot be read or edited without rewriting what it holds")}, {QStringLiteral("ai.tool.list-directory"), QStringLiteral("List the entries of a directory inside the task working directory")}, {QStringLiteral("ai.error.tool-argument-value"), QStringLiteral("The tool %1 received an unusable value for the argument %2")}, {QStringLiteral("ai.error.tool-argument-missing"), QStringLiteral("The tool %1 requires the argument %2")}, {QStringLiteral("ai.error.tool-arguments-unreadable"), QStringLiteral("The arguments of %1 could not be read as JSON. They arrived as: %2. Call the tool again with valid JSON arguments.")}, {QStringLiteral("ai.error.tool-argument-type"), QStringLiteral("The tool %1 expects the argument %2 to be of type %3")}, {QStringLiteral("ai.error.tool-path-missing"), QStringLiteral("The path %1 does not exist")}, {QStringLiteral("ai.error.destination-unavailable"), QStringLiteral("The plugin that opens that folder is not available")}, {QStringLiteral("ai.error.tool-workdir-none"), QStringLiteral("This task declares no working directory, so it has no file access")}, {QStringLiteral("ai.error.tool-workdir-unavailable"), QStringLiteral("The working directory %1 is unavailable")}, {QStringLiteral("ai.error.tool-path-outside"), QStringLiteral("The path %1 is outside the working directory %2")}, {QStringLiteral("ai.error.tool-repeated"), QStringLiteral("The agent repeated the tool %1 without progress")}, {QStringLiteral("ai.stop-reason.answered"), QStringLiteral("Answered")}, {QStringLiteral("ai.stop-reason.iteration-limit"), QStringLiteral("Stopped at the iteration limit")}, {QStringLiteral("ai.stop-reason.output-budget"), QStringLiteral("Stopped at the answer budget")}, {QStringLiteral("ai.stop-reason.tool-repetition"), QStringLiteral("Stopped after repeating one tool")}, {QStringLiteral("ai.stop-reason.cancelled"), QStringLiteral("Stopped by the reader")}, {QStringLiteral("ai.stop-reason.failed"), QStringLiteral("Failed")}, {QStringLiteral("ai.agent.final-turn"), QStringLiteral("This is the last turn of this run. Answer now with what you already have and do not call another tool.")}, {QStringLiteral("ai.tool.picture-not-sent"), QStringLiteral("The picture of this result was not sent because the selected model does not read images.")}, {QStringLiteral("ai.error.tool-repeated-reason"), QStringLiteral("The agent repeated the tool %1 without progress, and it kept answering: %2")}, {QStringLiteral("ai.error.tool-unknown"), QStringLiteral("The model asked for an unknown tool %1")}, {QStringLiteral("ai.error.tool-deadline"), QStringLiteral("The tool %1 did not answer in time")}, {QStringLiteral("ai.error.tool-cancelled"), QStringLiteral("The tool was stopped with the task")}, {QStringLiteral("ai.error.output-truncated"), QStringLiteral("The model stopped at its output budget of %1 tokens, so its answer is incomplete")}, {QStringLiteral("ai.settings.execution"), QStringLiteral("Execution")}, {QStringLiteral("ai.conversation.back"), QStringLiteral("Close the conversation")}, {QStringLiteral("ai.conversation.send"), QStringLiteral("Send")}, {QStringLiteral("ai.conversation.placeholder"), QStringLiteral("Write a message, Shift and Enter for a new line")}, {QStringLiteral("ai.conversation.empty"), QStringLiteral("Send a message or run the task to start this conversation")}, {QStringLiteral("ai.conversation.reset"), QStringLiteral("Reset Conversation")}, {QStringLiteral("ai.conversation.reset-title"), QStringLiteral("Reset Conversation")}, {QStringLiteral("ai.conversation.reset-message"), QStringLiteral("Clear this conversation?")}, {QStringLiteral("ai.conversation.reset-detail"), QStringLiteral("The messages and the runs this task recorded are removed permanently.")}, {QStringLiteral("ai.settings.group-connections"), QStringLiteral("AI Connections")}, {QStringLiteral("ai.settings.group-providers"), QStringLiteral("AI Providers")}, {QStringLiteral("ai.settings.group-agents"), QStringLiteral("AI Agents")}, {QStringLiteral("ai.settings.group-tools"), QStringLiteral("AI Tools")}, {QStringLiteral("ai.settings.group-general"), QStringLiteral("AI General")}, {QStringLiteral("ai.settings.agents"), QStringLiteral("Agents")}, {QStringLiteral("ai.agent.add"), QStringLiteral("New Agent")}, {QStringLiteral("ai.agent.edit"), QStringLiteral("Edit Agent")}, {QStringLiteral("ai.agent.remove"), QStringLiteral("Remove Agent")}, {QStringLiteral("ai.agent.remove-title"), QStringLiteral("Remove Agent")}, {QStringLiteral("ai.agent.remove-message"), QStringLiteral("Remove this agent?")}, {QStringLiteral("ai.agent.remove-detail"), QStringLiteral("%1 tasks are handed to this agent and will stop with an error until another one is chosen.")}, {QStringLiteral("ai.agent.identifier"), QStringLiteral("Identifier")}, {QStringLiteral("ai.agent.identifier-hint"), QStringLiteral("Leave it empty and it is spelled from the name.")}, {QStringLiteral("ai.agent.identifier-placeholder"), QStringLiteral("Lowercase letters, numbers and hyphens")}, {QStringLiteral("ai.agent.name"), QStringLiteral("Name")}, {QStringLiteral("ai.agent.description"), QStringLiteral("Description")}, {QStringLiteral("ai.agent.description-placeholder"), QStringLiteral("What this agent is expert at")}, {QStringLiteral("ai.agent.connection"), QStringLiteral("Model connection")}, {QStringLiteral("ai.agent.maximum-iterations"), QStringLiteral("Maximum iterations")}, {QStringLiteral("ai.agent.system-prompt"), QStringLiteral("System prompt")}, {QStringLiteral("ai.agent.system-prompt-placeholder"), QStringLiteral("The instructions this agent always receives")}, {QStringLiteral("ai.agent.insert-template"), QStringLiteral("Insert template")}, {QStringLiteral("ai.agent.show-tags"), QStringLiteral("Available tags")}, {QStringLiteral("ai.agent.empty"), QStringLiteral("No agent is configured")}, {QStringLiteral("ai.task.agent"), QStringLiteral("Agent")}, {QStringLiteral("ai.validation.agent-identifier"), QStringLiteral("The identifier must use lowercase letters, numbers and hyphens")}, {QStringLiteral("ai.validation.agent-duplicate"), QStringLiteral("Another agent already uses this identifier")}, {QStringLiteral("ai.validation.agent-name"), QStringLiteral("Enter a name for this agent")}, {QStringLiteral("ai.validation.agent-prompt"), QStringLiteral("Enter the system prompt this agent always receives")}, {QStringLiteral("ai.validation.agent-tag"), QStringLiteral("The system prompt carries a tag nobody declares: %1")}, {QStringLiteral("ai.validation.agent-missing"), QStringLiteral("Create an agent in the AI settings and choose it here")}, {QStringLiteral("ai.error.agent-removed"), QStringLiteral("The agent %1 was removed, so this task has nobody to run it")}, {QStringLiteral("ai.error.agent-save"), QStringLiteral("The agents could not be saved")}, {QStringLiteral("ai.error.connection-in-use"), QStringLiteral("The agent %1 runs on this connection. Point that agent at another connection before removing this one.")}, {QStringLiteral("ai.error.conversation-save"), QStringLiteral("The conversation could not be recorded")}, {QStringLiteral("ai.tag.system-prompt-data"), QStringLiteral("The working directory, the environment, the context files and the skills of the task")}, {QStringLiteral("ai.tag.agent-name"), QStringLiteral("The name of this agent")}, {QStringLiteral("ai.tag.agent-description"), QStringLiteral("The description of this agent")}, {QStringLiteral("ai.tag.task-title"), QStringLiteral("The title of the task")}, {QStringLiteral("ai.tag.task-description"), QStringLiteral("The description of the task")}, {QStringLiteral("ai.tag.task-prompt"), QStringLiteral("The prompt of the task")}, {QStringLiteral("ai.tag.task-workdir"), QStringLiteral("The working directory of the task")}, {QStringLiteral("ai.tag.task-issue-url"), QStringLiteral("The issue address of the task")}, {QStringLiteral("ai.tag.date-time"), QStringLiteral("The local date and time")}, {QStringLiteral("ai.tag.date-time-utc"), QStringLiteral("The date and time in UTC")}, {QStringLiteral("ai.tag.time-zone"), QStringLiteral("The time zone of this machine")}, {QStringLiteral("ai.tag.locale"), QStringLiteral("The locale of this machine")}, {QStringLiteral("ai.tag.language"), QStringLiteral("The language of this machine")}, {QStringLiteral("ai.tag.operating-system"), QStringLiteral("The operating system this machine runs")}, {QStringLiteral("ai.tag.user-name"), QStringLiteral("The user this machine reports")}, {QStringLiteral("ai.tag.home-directory"), QStringLiteral("The home directory of the user")}, {QStringLiteral("ai.tag.tools"), QStringLiteral("The names of the tools the agent may call")}, {QStringLiteral("ai.tag.model"), QStringLiteral("The provider and model this run speaks to")}, {QStringLiteral("ai.tag.model-traits"), QStringLiteral("What the selected model declares it can do")}, {QStringLiteral("ai.tag.vision"), QStringLiteral("Whether the selected model reads an image")}, {QStringLiteral("ai.tag.search"), QStringLiteral("Whether a web search service is configured")}, {QStringLiteral("ai.tag.speech"), QStringLiteral("Whether a speech service is configured")}, {QStringLiteral("ai.tag.servers"), QStringLiteral("The Model Context Protocol servers that are connected right now")}, {QStringLiteral("ai.tag.context-window"), QStringLiteral("How many tokens the window of the selected model holds")}, {QStringLiteral("ai.tag.output-budget"), QStringLiteral("How many tokens the answer of one turn may use")}, {QStringLiteral("ai.capability.vision-yes"), QStringLiteral("You read images. Use read_image on a picture that matters to the task.")}, {QStringLiteral("ai.capability.vision-no"), QStringLiteral("You do not read images. Do not ask for one, work from the text instead.")}, {QStringLiteral("ai.capability.search-yes"), QStringLiteral("You can search the web through %1.")}, {QStringLiteral("ai.capability.search-no"), QStringLiteral("No web search service is configured, so web_search will refuse.")}, {QStringLiteral("ai.capability.speech-yes"), QStringLiteral("You can generate speech through %1.")}, {QStringLiteral("ai.capability.speech-no"), QStringLiteral("No speech service is configured, so generate_speech will refuse.")}, {QStringLiteral("ai.capability.servers"), QStringLiteral("These servers are connected and their tools are yours to call: %1.")}, {QStringLiteral("ai.capability.servers-none"), QStringLiteral("No server is connected, so only the tools of this application are available.")}, {QStringLiteral("ai.tag.skills"), QStringLiteral("The skills the working directory declares")}, {QStringLiteral("ai.tag.context-files"), QStringLiteral("The published context files of the working directory")}, {QStringLiteral("ai.settings.rate-limits"), QStringLiteral("Rate limits")}, {QStringLiteral("ai.settings.provider-selection"), QStringLiteral("Provider")}, {QStringLiteral("ai.settings.provider-scope"), QStringLiteral("Settings for")}, {QStringLiteral("ai.settings.rate-limit-interval"), QStringLiteral("Delay between requests in milliseconds")}, {QStringLiteral("ai.settings.rate-limit-per-minute"), QStringLiteral("Maximum requests per minute")}, {QStringLiteral("ai.settings.rate-limit-concurrent"), QStringLiteral("Maximum requests at the same time")}, {QStringLiteral("ai.phase.throttled"), QStringLiteral("Waiting for the provider")}, {QStringLiteral("ai.log-kind.throttled"), QStringLiteral("Waiting before the request")}, {QStringLiteral("ai.log.rate-limit-delay"), QStringLiteral("Waiting %1 ms before reaching the provider")}, {QStringLiteral("ai.log.retry-delay"), QStringLiteral("The service refused the request, trying again in %1 ms")}, {QStringLiteral("ai.settings.maximum-iterations"), QStringLiteral("Maximum agent iterations")}, {QStringLiteral("ai.settings.command-timeout"), QStringLiteral("Command time limit in seconds")}, {QStringLiteral("ai.task.execution-kind"), QStringLiteral("Execution")}, {QStringLiteral("ai.task.kind-agent"), QStringLiteral("AI agent")}, {QStringLiteral("ai.task.kind-command"), QStringLiteral("Command")}, {QStringLiteral("ai.task.workdir"), QStringLiteral("Working directory")}, {QStringLiteral("ai.task.choose-workdir"), QStringLiteral("Choose Working Directory")}, {QStringLiteral("ai.task.command"), QStringLiteral("Command")}, {QStringLiteral("ai.task.command-timeout"), QStringLiteral("Time limit in seconds")}, {QStringLiteral("ai.task.unlimited-hint"), QStringLiteral("Zero means unlimited")}, {QStringLiteral("ai.validation.command"), QStringLiteral("Enter the command this task runs")}, {QStringLiteral("ai.validation.workdir"), QStringLiteral("Choose an existing absolute working directory")}, {QStringLiteral("ai.error.exit-code"), QStringLiteral("The command exited with code %1")}, {QStringLiteral("ai.error.command-timeout"), QStringLiteral("The command exceeded its time limit")}, {QStringLiteral("ai.error.command-output-too-large"), QStringLiteral("The command produced more output than is permitted")}, {QStringLiteral("ai.error.command-workdir-invalid"), QStringLiteral("The working directory of the command is unavailable")}, {QStringLiteral("ai.error.command-start-failed"), QStringLiteral("The command could not be started")}, {QStringLiteral("ai.error.command-crashed"), QStringLiteral("The command terminated abnormally")}, {QStringLiteral("ai.error.tool-answer-truncated"), QStringLiteral("The service answered with more than the permitted size, so no file was written")}, {QStringLiteral("ai.error.endpoint-unavailable"), QStringLiteral("The selected provider does not answer this kind of request")}, {QStringLiteral("ai.phase.calling-tool"), QStringLiteral("Calling %1")}, {QStringLiteral("ai.phase.tool-count"), QStringLiteral("%1 tools")}, {QStringLiteral("ai.phase.compacting"), QStringLiteral("Summarizing the earlier conversation")}, {QStringLiteral("ai.phase.running"), QStringLiteral("Running the command")}, {QStringLiteral("ai.log-kind.tool-called"), QStringLiteral("Tool called")}, {QStringLiteral("ai.log-kind.tool-returned"), QStringLiteral("Tool returned")}, {QStringLiteral("ai.log-kind.iteration"), QStringLiteral("Agent iteration")}, {QStringLiteral("ai.log.event"), QStringLiteral("Event")}, {QStringLiteral("ai.log.detail"), QStringLiteral("Detail")}, {QStringLiteral("ai.log.usage"), QStringLiteral("%1 input tokens, %2 output tokens, finished as %3")}, {QStringLiteral("ai.log-kind.started"), QStringLiteral("Execution started")}, {QStringLiteral("ai.log-kind.request-sent"), QStringLiteral("Request sent to the model")}, {QStringLiteral("ai.log-kind.first-token"), QStringLiteral("First content received")}, {QStringLiteral("ai.log-kind.response-received"), QStringLiteral("Full response received")}, {QStringLiteral("ai.log-kind.usage-reported"), QStringLiteral("Token usage reported")}, {QStringLiteral("ai.log-kind.succeeded"), QStringLiteral("Execution succeeded")}, {QStringLiteral("ai.log-kind.failed"), QStringLiteral("Execution failed")}, {QStringLiteral("ai.log-kind.cancelled"), QStringLiteral("Execution cancelled")}, {QStringLiteral("ai.phase.idle"), QStringLiteral("Idle")}, {QStringLiteral("ai.phase.queued"), QStringLiteral("Waiting for capacity")}, {QStringLiteral("ai.phase.sending"), QStringLiteral("Waiting for the model")}, {QStringLiteral("ai.phase.streaming"), QStringLiteral("Receiving the response")}, {QStringLiteral("ai.validation.api-key"), QStringLiteral("Enter the API key this provider requires")}, {QStringLiteral("ai.validation.model"), QStringLiteral("Choose the model this provider should run")}, {QStringLiteral("ai.validation.output-budget-unknown"), QStringLiteral("The catalog does not declare the model %1, so enter the maximum output tokens it accepts instead of leaving it at zero")}, {QStringLiteral("ai.validation.output-budget-whole-window"), QStringLiteral("The model %1 declares its whole window as its maximum answer, so enter the tokens to leave for that answer instead of zero")}, {QStringLiteral("ai.output.none"), QStringLiteral("Select an execution to see its output")}, {QStringLiteral("ai.output.running"), QStringLiteral("The execution is still running")}, {QStringLiteral("ai.output.failed"), QStringLiteral("The execution failed before the model returned any text")}, {QStringLiteral("ai.output.cancelled"), QStringLiteral("The execution was cancelled before the model returned any text")}, {QStringLiteral("ai.output.empty"), QStringLiteral("The model returned no text")}, {QStringLiteral("ai.agent.environment"), QStringLiteral("This is the machine and the moment you are running in. Use it whenever the task depends on the current date, the language or the paths of this system.")}, {QStringLiteral("ai.log.open-payload"), QStringLiteral("View content")}, {QStringLiteral("ai.task.schedule-done"), QStringLiteral("Ran on %1")}, {QStringLiteral("ai.tool.create-directory"), QStringLiteral("Create a directory inside the task working directory")}, {QStringLiteral("ai.tool.move-path"), QStringLiteral("Move or rename a file or directory inside the task working directory")}, {QStringLiteral("ai.tool.copy-file"), QStringLiteral("Copy a file inside the task working directory")}, {QStringLiteral("ai.tool.remove-path"), QStringLiteral("Remove a file, an empty directory, or a directory with everything inside it when asked recursively")}, {QStringLiteral("ai.error.tool-remove-not-empty"), QStringLiteral("The directory %1 is not empty, so ask again with recursive set to true to remove what is inside it")}, {QStringLiteral("ai.tool.describe-path"), QStringLiteral("Report the type, size, modification time and permissions of a path")}, {QStringLiteral("ai.tool.search-files"), QStringLiteral("Search files by name pattern and optionally by the text they contain")}, {QStringLiteral("ai.tool.run-command"), QStringLiteral("Run a command inside the task working directory and return its output")}, {QStringLiteral("ai.tool.list-voices"), QStringLiteral("List the voices the configured speech service publishes")}, {QStringLiteral("ai.tool.command-no-output"), QStringLiteral("The command produced no output")}, {QStringLiteral("ai.error.tool-no-match"), QStringLiteral("No entry matches the search")}, {QStringLiteral("ai.error.tool-sandbox-missing"), QStringLiteral("This task declares no working directory, so it cannot run a command")}, {QStringLiteral("ai.error.tool-voices-empty"), QStringLiteral("The speech service published no voice")}, {QStringLiteral("ai.settings.speech-provider"), QStringLiteral("Service")}, {QStringLiteral("ai.settings.speech-voice-placeholder"), QStringLiteral("Identifier of the voice to speak with")}, {QStringLiteral("ai.settings.search-instance"), QStringLiteral("Instance address")}, {QStringLiteral("ai.settings.search-instance-placeholder"), QStringLiteral("Address of your SearXNG instance")}, {QStringLiteral("ai.tool.list-mcp-resources"), QStringLiteral("List the resources every connected server publishes")}, {QStringLiteral("ai.tool.read-mcp-resource"), QStringLiteral("Read one resource published by a connected server")}, {QStringLiteral("ai.tool.list-mcp-prompts"), QStringLiteral("List the prompts every connected server publishes")}, {QStringLiteral("ai.tool.read-mcp-prompt"), QStringLiteral("Load one prompt published by a connected server")}, {QStringLiteral("ai.error.mcp-none"), QStringLiteral("No server is connected")}, {QStringLiteral("ai.settings.refresh-models"), QStringLiteral("Refresh the models this provider publishes")}, {QStringLiteral("ai.validation.no-model-published"), QStringLiteral("The provider published no model for this credential")}, {QStringLiteral("ai.agent.summarize"), QStringLiteral("Summarize the conversation below into the facts, decisions and results the agent still needs. Answer with the summary only.")}, {QStringLiteral("ai.agent.summary"), QStringLiteral("Summary of the earlier part of this run:\n\n%1")}, {QStringLiteral("ai.tool.web-search"), QStringLiteral("Search the web and return the title, address and summary of each result")}, {QStringLiteral("ai.error.tool-search-unconfigured"), QStringLiteral("Web search needs a query and a configured search provider")}, {QStringLiteral("ai.search.no-result"), QStringLiteral("The search returned no result")}, {QStringLiteral("ai.settings.search"), QStringLiteral("Web search")}, {QStringLiteral("ai.settings.search-provider"), QStringLiteral("Service")}, {QStringLiteral("ai.settings.search-key"), QStringLiteral("API key")}, {QStringLiteral("ai.search.brave"), QStringLiteral("Brave Search")}, {QStringLiteral("ai.search.tavily"), QStringLiteral("Tavily")}, {QStringLiteral("ai.search.searxng"), QStringLiteral("SearXNG")}, {QStringLiteral("ai.settings.mcp"), QStringLiteral("MCP servers")}, {QStringLiteral("ai.mcp.add"), QStringLiteral("New Server")}, {QStringLiteral("ai.mcp.edit"), QStringLiteral("Edit Server")}, {QStringLiteral("ai.mcp.remove"), QStringLiteral("Remove Server")}, {QStringLiteral("ai.mcp.remove-title"), QStringLiteral("Remove MCP Server")}, {QStringLiteral("ai.mcp.remove-message"), QStringLiteral("Remove this server and the tools it publishes?")}, {QStringLiteral("ai.mcp.identifier"), QStringLiteral("Identifier")}, {QStringLiteral("ai.mcp.identifier-placeholder"), QStringLiteral("Lowercase letters, numbers and hyphens")}, {QStringLiteral("ai.mcp.transport"), QStringLiteral("Transport")}, {QStringLiteral("ai.mcp.transport-stdio"), QStringLiteral("Local process")}, {QStringLiteral("ai.mcp.transport-http"), QStringLiteral("HTTP")}, {QStringLiteral("ai.mcp.command"), QStringLiteral("Command")}, {QStringLiteral("ai.mcp.arguments"), QStringLiteral("Arguments")}, {QStringLiteral("ai.mcp.workdir"), QStringLiteral("Working directory")}, {QStringLiteral("ai.mcp.address"), QStringLiteral("Address")}, {QStringLiteral("ai.mcp.api-key"), QStringLiteral("API key")}, {QStringLiteral("ai.mcp.roots"), QStringLiteral("Roots")}, {QStringLiteral("ai.mcp.roots-placeholder"), QStringLiteral("One absolute directory per line")}, {QStringLiteral("ai.mcp.sampling"), QStringLiteral("Allow the server to ask the model")}, {QStringLiteral("ai.mcp.sampling-tokens"), QStringLiteral("Maximum tokens per server request")}, {QStringLiteral("ai.mcp.target"), QStringLiteral("Target")}, {QStringLiteral("ai.mcp.tools"), QStringLiteral("Tools")}, {QStringLiteral("ai.mcp.empty"), QStringLiteral("No server is registered")}, {QStringLiteral("ai.validation.mcp-identifier"), QStringLiteral("The identifier must use lowercase letters, numbers and hyphens")}, {QStringLiteral("ai.validation.mcp-duplicate"), QStringLiteral("Another server already uses this identifier")}, {QStringLiteral("ai.validation.mcp-command"), QStringLiteral("Enter the command that starts the server")}, {QStringLiteral("ai.validation.mcp-address"), QStringLiteral("Enter a valid HTTP or HTTPS address for this server")}, {QStringLiteral("ai.error.mcp-save"), QStringLiteral("The MCP servers could not be saved")}, {QStringLiteral("ai.error.mcp-unavailable"), QStringLiteral("The server publishing the tool %1 is unavailable")}, {QStringLiteral("ai.settings.connections"), QStringLiteral("Model connections")}, {QStringLiteral("ai.settings.default-connection"), QStringLiteral("Default connection")}, {QStringLiteral("ai.connection.add"), QStringLiteral("New Connection")}, {QStringLiteral("ai.connection.edit"), QStringLiteral("Edit Connection")}, {QStringLiteral("ai.connection.remove"), QStringLiteral("Remove Connection")}, {QStringLiteral("ai.connection.remove-title"), QStringLiteral("Remove Model Connection")}, {QStringLiteral("ai.connection.remove-message"), QStringLiteral("Remove this connection? Tasks that run on it will stop until they are pointed at another one.")}, {QStringLiteral("ai.connection.display-name"), QStringLiteral("Display name")}, {QStringLiteral("ai.connection.display-name-hint"), QStringLiteral("Leave it empty and the provider and the model are shown.")}, {QStringLiteral("ai.connection.display-name-placeholder"), QStringLiteral("Name shown when a task chooses this connection")}, {QStringLiteral("ai.connection.address"), QStringLiteral("Service address")}, {QStringLiteral("ai.connection.address-placeholder"), QStringLiteral("Address of your local service")}, {QStringLiteral("ai.connection.empty"), QStringLiteral("No connection is configured")}, {QStringLiteral("ai.connection.extra-parameters"), QStringLiteral("Extra parameters")}, {QStringLiteral("ai.connection.extra-parameters-description"), QStringLiteral("A JSON object merged over the parameters above when the request is sent. Use it for anything the service accepts that is not listed here, write a dotted key to reach a nested field, and use null to remove a field the service must not receive.")}, {QStringLiteral("ai.connection.extra-parameters-placeholder"), QStringLiteral("{\"seed\": 42, \"top_k\": 40}")}, {QStringLiteral("ai.validation.connection-duplicate"), QStringLiteral("This provider and model pair is already configured")}, {QStringLiteral("ai.validation.connection-address"), QStringLiteral("Enter a valid HTTP or HTTPS address for this service")}, {QStringLiteral("ai.validation.connection-missing"), QStringLiteral("Configure a model connection in the AI settings and choose it here")}, {QStringLiteral("ai.validation.extra-parameters-object"), QStringLiteral("The extra parameters must be a JSON object")}, {QStringLiteral("ai.validation.extra-parameters-syntax"), QStringLiteral("The extra parameters are not valid JSON: %1 at position %2")}, {QStringLiteral("ai.validation.extra-parameters-key"), QStringLiteral("An extra parameter name cannot be empty")}, {QStringLiteral("ai.error.connection-save"), QStringLiteral("The model connections could not be saved")}, {QStringLiteral("ai.error.connection-missing"), QStringLiteral("The connection %1 is not configured, so the agent of this task cannot run")}, {QStringLiteral("ai.error.service-save"), QStringLiteral("The service settings could not be saved")}, {QStringLiteral("ai.error.settings-save"), QStringLiteral("The AI settings could not be saved")}, {QStringLiteral("ai.error.workspace-save"), QStringLiteral("The workspace could not be saved")}, {QStringLiteral("ai.error.task-run"), QStringLiteral("The task could not be started or stopped")}, {QStringLiteral("ai.error.execution-save"), QStringLiteral("The run could not be recorded")}, {QStringLiteral("ai.error.log-save"), QStringLiteral("The run log could not be recorded")}, {QStringLiteral("ai.error.task-save"), QStringLiteral("The task could not be saved")}, {QStringLiteral("ai.error.schedule-save"), QStringLiteral("The schedule could not be advanced")}, {QStringLiteral("ai.error.server-failed"), QStringLiteral("A Model Context Protocol server reported a failure")},
        };
    }

    static TranslationEntries portuguese() {
        return {{QStringLiteral("ai.tool-title.read-file"), QStringLiteral("Ler arquivo")}, {QStringLiteral("ai.template.application-name"), QStringLiteral("Criação de aplicativos")}, {QStringLiteral("ai.template.application-description"), QStringLiteral("Cria e mantém um aplicativo, da organização ao empacotamento.")}, {QStringLiteral("ai.template.application-body"), QStringLiteral("Você é {{AGENT_NAME}}. {{AGENT_DESCRIPTION}}\n\n{{SYSTEM_PROMPT_DATA}}\n\n# A tarefa\n\n{{TASK_TITLE}}\n{{TASK_DESCRIPTION}}\n\nDiretório de trabalho: {{TASK_WORKDIR}}\nIssue: {{TASK_ISSUE_URL}}\n\n# O que esta execução realmente tem\n\nModelo {{MODEL}} declarando {{MODEL_TRAITS}}, uma janela de contexto de {{CONTEXT_WINDOW}} e um orçamento de resposta de {{OUTPUT_BUDGET}}.\nFerramentas: {{TOOLS}}\n{{VISION}}\n{{SEARCH}}\n{{SPEECH}}\n{{SERVERS}}\n{{SKILLS}}\n{{CONTEXT_FILES}}\n\n# Como você constrói um aplicativo\n\nLeia o projeto antes de mudá-lo. Descubra como ele é construído, como é executado e como é testado, e\nsiga esses caminhos em vez de inventar os seus. As convenções que já estão no código vencem as suas\npreferências.\n\nOrganização. Mantenha uma responsabilidade por módulo e deixe a estrutura de diretórios dizer o que\ncada parte possui. As dependências apontam numa direção: o domínio não depende da interface nem do\narmazenamento. Um nome diz o que a coisa é, então um módulo bem nomeado não precisa de comentário\nexplicando que existe.\n\nDependências e build. Prefira o que a biblioteca padrão e as dependências existentes já oferecem.\nAcrescente uma dependência só quando ela remove trabalho real, e fixe a versão como o projeto fixa as\noutras. Toda mudança deve deixar o build verde com os avisos ainda tratados como erros.\n\nEstado. Torne o estado inválido irrepresentável onde a linguagem permitir. Mantenha estado mutável em\npoucos lugares e deixe a posse explícita em vez de subentendida. O que é compartilhado entre threads\né imutável ou protegido, nunca apenas presumido seguro.\n\nTratamento de erros. Todo caminho de falha devolve algo sobre o que o chamador possa agir, com um\ncódigo estável e uma mensagem. Nunca engula uma falha, nunca a transforme num valor vazio e nunca\ndeixe o estado pela metade quando um passo falha: ou a operação inteira acontece, ou nenhuma parte\ndela acontece.\n\nAssincronia. Nada que possa esperar por armazenamento, processo, rede ou outra thread roda na thread\nque desenha. O cancelamento é explícito e idempotente. Um callback ligado a um objeto que pode ser\ndestruído é protegido, para que uma conclusão nunca alcance algo que já se foi.\n\nPersistência. Valide tudo o que for lido de volta antes de confiar. Um valor armazenado que o código\nnão consegue usar é um padrão declarado ou uma falha explícita, nunca um palpite silencioso. Uma\nmudança de esquema é aditiva e preserva o que o leitor já tinha guardado.\n\nTestes. Todo comportamento que você acrescenta carrega casos para o caminho de sucesso, o de falha e\no limite. Um teste afirma o que o produto garante, não o que uma plataforma costuma fazer. Execute a\nsuíte e leia a saída, e nunca reporte uma mudança como pronta com um caso vermelho.\n\nEmpacotamento. Saiba como o produto é empacotado e confirme que sua mudança sobrevive a isso. Uma\nmudança que compila mas não pode ser distribuída não está terminada.\n\nRelate o que fez, o que verificou e o que deixou de fora deliberadamente.")}, {QStringLiteral("ai.template.code-review-name"), QStringLiteral("Revisão de código")}, {QStringLiteral("ai.template.code-review-description"), QStringLiteral("Encontra defeitos reais, de falhas de memória e vazamentos a corridas e crescimento sem limite.")}, {QStringLiteral("ai.template.code-review-body"), QStringLiteral("Você é {{AGENT_NAME}}. {{AGENT_DESCRIPTION}}\n\n{{SYSTEM_PROMPT_DATA}}\n\n# A tarefa\n\n{{TASK_TITLE}}\n{{TASK_DESCRIPTION}}\n\nDiretório de trabalho: {{TASK_WORKDIR}}\nIssue: {{TASK_ISSUE_URL}}\n\n# O que esta execução realmente tem\n\nModelo {{MODEL}} declarando {{MODEL_TRAITS}}, uma janela de contexto de {{CONTEXT_WINDOW}} e um orçamento de resposta de {{OUTPUT_BUDGET}}.\nFerramentas: {{TOOLS}}\n{{VISION}}\n{{SEARCH}}\n{{SPEECH}}\n{{SERVERS}}\n{{SKILLS}}\n{{CONTEXT_FILES}}\n\n# Como você revisa código\n\nVocê procura defeitos que realmente machucariam e os relata com a entrada e o estado que os produzem.\nEstilo não é um achado, a menos que o projeto o declare. Uma mudança que você não consegue justificar\ncomo correção de algo não é uma mudança.\n\nCorreção. Verifique todos os limites: a coleção vazia, o elemento único, o último índice, o valor\nexatamente no limite e o seguinte. Verifique estouro de inteiro e o truncamento de um tipo mais largo\npara um mais estreito. Verifique toda comparação entre um valor com sinal e um sem sinal.\n\nMemória. Procure leitura ou escrita além do fim de um buffer, uso depois da liberação, ponteiro para\ndentro de um contêiner que está prestes a crescer, referência a um temporário que morre no fim da\nexpressão e iterador mantido através de uma mutação daquilo que ele percorre.\n\nPosse e tempo de vida. Descubra quem é dono de cada alocação e confirme que ela é liberada em todos os\ncaminhos, incluindo os retornos antecipados e os caminhos de falha. Procure ponteiro cru com posse\nonde caberia um inteligente, liberação dupla e objeto que sobrevive a algo cuja referência ele guarda.\n\nVazamentos. Procure alocação que nenhum caminho libera, handle ou descritor nunca fechado, inscrição\nou conexão nunca desconectada e contêiner que só cresce.\n\nConcorrência. Encontre estado mutável compartilhado alcançado por mais de uma thread e confirme que\ntodo acesso está sob o mesmo lock. Procure valor lido sob lock mas escrito fora dele, condição\nsinalizada sem o estado ter sido mudado sob aquele lock, notificação perdida e dois locks tomados em\nordens diferentes em caminhos diferentes. Verifique se um callback entregue em outra thread não\nalcança um objeto que já está sendo destruído.\n\nCaminhos de erro. Confirme que toda falha é verificada, que nenhum resultado é descartado em silêncio\ne que uma mudança de vários passos ou acontece inteira ou é revertida. Procure falha transformada em\nvalor vazio.\n\nLimites de recurso. Encontre o que cresce a partir do que o chamador decide: recursão cuja\nprofundidade vem da entrada, leitura sem limite, alocação a partir de um campo de tamanho.\n\nTestes. Diga com clareza qual dos casos que você encontrou não está coberto e o que um caso que o\npegue precisaria fazer.\n\nRelate cada achado como o defeito, a entrada ou o entrelaçamento concreto que o dispara e a menor\ncorreção. Ordene-os pelo custo que têm quando acontecem.")}, {QStringLiteral("ai.template.security-name"), QStringLiteral("Revisão de segurança")}, {QStringLiteral("ai.template.security-description"), QStringLiteral("Revisa o aplicativo onde está rodando contra atacantes, invasores e brechas.")}, {QStringLiteral("ai.template.security-body"), QStringLiteral("Você é {{AGENT_NAME}}. {{AGENT_DESCRIPTION}}\n\n{{SYSTEM_PROMPT_DATA}}\n\n# A tarefa\n\n{{TASK_TITLE}}\n{{TASK_DESCRIPTION}}\n\nDiretório de trabalho: {{TASK_WORKDIR}}\nIssue: {{TASK_ISSUE_URL}}\n\n# O que esta execução realmente tem\n\nModelo {{MODEL}} declarando {{MODEL_TRAITS}}, uma janela de contexto de {{CONTEXT_WINDOW}} e um orçamento de resposta de {{OUTPUT_BUDGET}}.\nFerramentas: {{TOOLS}}\n{{VISION}}\n{{SEARCH}}\n{{SPEECH}}\n{{SERVERS}}\n{{SKILLS}}\n{{CONTEXT_FILES}}\n\n# Como você revisa segurança\n\nVocê revisa o aplicativo que está no diretório de trabalho procurando por onde um atacante real\nentraria. Você relata o que encontra. Você nunca explora um sistema em uso, nunca extrai nada e nunca\nenfraquece uma proteção para provar um ponto.\n\nComece mapeando a superfície de ataque: todo lugar onde dados cruzam uma fronteira. Endpoints de\nrede, argumentos de linha de comando, variáveis de ambiente, arquivos que o produto lê, mensagens\nentre processos, conteúdo enviado, o banco de dados e o que um plugin ou extensão possa contribuir.\n\nEntrada. Todo valor que vem de fora é não confiável até ser validado contra o que ele pode ser.\nProcure verificações de tamanho, tipo, faixa e codificação, e a ausência delas. Recuse em vez de\nsanear quando puder, porque um saneador que dá para driblar é pior que uma recusa.\n\nInjeção. Verifique toda consulta montada por concatenação, todo comando montado a partir de uma\nstring, todo caminho montado com entrada do usuário, todo template renderizado com conteúdo sem\nescape e toda desserialização de dados que o produto não escreveu. Consulta parametrizada e vetor de\nargumentos são as respostas.\n\nCaminhos. Verifique se um caminho é julgado por onde ele realmente cai, e não por como foi escrito,\nde modo que um traversal e um link simbólico que sai da raiz permitida sejam ambos recusados.\nVerifique se o ancestral existente mais profundo é resolvido antes do teste de contenção.\n\nAutenticação e autorização. Procure a verificação que falta, não a que está lá. Toda ação privilegiada\nconfere quem está pedindo do lado do servidor, e nunca com base em algo que o cliente enviou.\nIdentificadores de sessão são imprevisíveis, rotacionados na mudança de privilégio e invalidados na\nsaída.\n\nSegredos. Encontre toda credencial na árvore, no histórico, na configuração, nos logs e nas mensagens\nde erro. Um segredo nunca deve ser registrado, nunca deve viajar numa URL e nunca deve ser comparado\ncom uma comparação que retorna cedo. Verifique o que o processo passa para os filhos que inicia.\n\nTransporte e armazenamento. Verifique certificados em vez de aceitar qualquer um. Prefira criptografia\nautenticada e uma função de derivação de chave publicada a qualquer coisa inventada localmente.\nVerifique o que o produto escreve em disco e com quais permissões.\n\nLimites de recurso. Todo buffer que cresce a partir do que alguém de fora envia precisa ser limitado\nantes de os bytes chegarem, não depois. Procure recursão sem limite cuja profundidade a entrada\ndecide, leituras sem prazo, contagem de conexões sem teto e qualquer coisa que aloque por um campo de\ntamanho.\n\nDependências. Verifique o que está fixado e o que está solto, e o que as versões declaradas\nnotoriamente carregam.\n\nRelate cada achado com onde está, o que um atacante faria com ele, quão grave é e a menor mudança que\no fecha. Diga com clareza o que você não conseguiu verificar e por quê.")}, {QStringLiteral("ai.template.website-name"), QStringLiteral("Criação de sites")}, {QStringLiteral("ai.template.website-description"), QStringLiteral("Cria e mantém um site, da estrutura à forma como ele é verificado.")}, {QStringLiteral("ai.template.website-body"), QStringLiteral("Você é {{AGENT_NAME}}. {{AGENT_DESCRIPTION}}\n\n{{SYSTEM_PROMPT_DATA}}\n\n# A tarefa\n\n{{TASK_TITLE}}\n{{TASK_DESCRIPTION}}\n\nDiretório de trabalho: {{TASK_WORKDIR}}\nIssue: {{TASK_ISSUE_URL}}\n\n# O que esta execução realmente tem\n\nModelo {{MODEL}} declarando {{MODEL_TRAITS}}, uma janela de contexto de {{CONTEXT_WINDOW}} e um orçamento de resposta de {{OUTPUT_BUDGET}}.\nFerramentas: {{TOOLS}}\n{{VISION}}\n{{SEARCH}}\n{{SPEECH}}\n{{SERVERS}}\n{{SKILLS}}\n{{CONTEXT_FILES}}\n\n# Como você constrói um site\n\nLeia o que já existe antes de escrever qualquer coisa. As convenções do projeto vencem as suas\npreferências, então siga a estrutura, os nomes e a formatação que os arquivos existentes estabelecem.\n\nEstrutura. Mantenha uma página por documento, ponha a marcação compartilhada nos parciais ou\ncomponentes que o projeto já usa e nunca duplique um bloco que poderia ser incluído. Separe conteúdo,\napresentação e comportamento: a marcação diz o que a página significa, o estilo decide como ela se\nparece e o script existe só para o que realmente precisa ser executado.\n\nMarcação. Escreva HTML semântico e válido. Use títulos em ordem e nunca pule um nível, listas para\nlistas, botões para ações e links para navegação. Toda imagem carrega um texto alternativo que diz\npara que ela serve, e uma imagem decorativa carrega um vazio. Todo campo de formulário tem rótulo. O\ndocumento declara seu idioma e seu conjunto de caracteres.\n\nEstilo. Prefira o sistema que o projeto já tem, seja CSS puro, um pré-processador ou um framework de\nutilitários. Use unidades relativas para o texto acompanhar as preferências de quem lê. Faça o\nleiaute com flexbox e grid em vez de floats ou posicionamento absoluto. Defina cor, espaçamento e\ntipografia como tokens e referencie-os, para que uma mudança aconteça num lugar só.\n\nResponsividade. Desenhe primeiro para a viewport estreita e acrescente o que a larga permite. Nada\npode rolar a página para os lados: tabelas largas, blocos de código e diagramas rolam dentro do\npróprio contêiner. Teste o leiaute num telefone pequeno, num tablet e numa tela larga antes de dar\npor pronto.\n\nAcessibilidade. Todo elemento interativo é alcançável e operável pelo teclado, e o anel de foco\ncontinua visível. A cor nunca carrega sentido sozinha. O contraste atende à razão publicada para\ntexto corrido e para texto grande. Respeite a preferência por menos movimento.\n\nDesempenho. Sirva imagens num formato moderno no tamanho em que aparecem, dê a elas dimensões\nexplícitas para a página não saltar durante o carregamento e carregue as de baixo da dobra de forma\npreguiçosa. Mantenha o caminho crítico pequeno, adie scripts que não são necessários para renderizar\ne não acrescente uma dependência para algo que poucas linhas resolvem.\n\nVerificação. Depois de cada mudança, leia de volta os arquivos que escreveu. Se o projeto tem build,\nexecute. Se tem linter, formatador ou testes, execute e corrija o que for reportado. Quando houver um\nservidor disponível, abra a página e confirme que o que você mudou realmente aparece. Nunca reporte\ntrabalho como concluído só porque foi escrito.\n\nRelate o que fez, o que verificou e o que deixou de fora deliberadamente.")}, {QStringLiteral("ai.tool-activity.read-file"), QStringLiteral("Lendo %1")}, {QStringLiteral("ai.tool-title.write-file"), QStringLiteral("Gravar arquivo")}, {QStringLiteral("ai.tool-activity.write-file"), QStringLiteral("Gravando %1")}, {QStringLiteral("ai.tool-title.edit-file"), QStringLiteral("Editar arquivo")}, {QStringLiteral("ai.tool-activity.edit-file"), QStringLiteral("Editando %1")}, {QStringLiteral("ai.tool-title.read-image"), QStringLiteral("Ver imagem")}, {QStringLiteral("ai.tool-activity.read-image"), QStringLiteral("Vendo %1")}, {QStringLiteral("ai.tool-title.list-directory"), QStringLiteral("Listar diretório")}, {QStringLiteral("ai.tool-activity.list-directory"), QStringLiteral("Listando %1")}, {QStringLiteral("ai.tool-title.create-directory"), QStringLiteral("Criar diretório")}, {QStringLiteral("ai.tool-activity.create-directory"), QStringLiteral("Criando %1")}, {QStringLiteral("ai.tool-title.move-path"), QStringLiteral("Mover")}, {QStringLiteral("ai.tool-activity.move-path"), QStringLiteral("Movendo %1")}, {QStringLiteral("ai.tool-title.copy-file"), QStringLiteral("Copiar")}, {QStringLiteral("ai.tool-activity.copy-file"), QStringLiteral("Copiando %1")}, {QStringLiteral("ai.tool-title.remove-path"), QStringLiteral("Remover")}, {QStringLiteral("ai.tool-activity.remove-path"), QStringLiteral("Removendo %1")}, {QStringLiteral("ai.tool-title.describe-path"), QStringLiteral("Descrever caminho")}, {QStringLiteral("ai.tool-activity.describe-path"), QStringLiteral("Descrevendo %1")}, {QStringLiteral("ai.tool-title.search-files"), QStringLiteral("Buscar arquivos")}, {QStringLiteral("ai.tool-activity.search-files"), QStringLiteral("Buscando por: %1")}, {QStringLiteral("ai.tool-title.run-command"), QStringLiteral("Executar comando")}, {QStringLiteral("ai.tool-activity.run-command"), QStringLiteral("Executando: %1")}, {QStringLiteral("ai.tool-title.fetch-url"), QStringLiteral("Buscar página")}, {QStringLiteral("ai.tool-activity.fetch-url"), QStringLiteral("Buscando %1")}, {QStringLiteral("ai.tool-title.web-search"), QStringLiteral("Busca web")}, {QStringLiteral("ai.tool-activity.web-search"), QStringLiteral("Buscando por: %1")}, {QStringLiteral("ai.tool-title.generate-image"), QStringLiteral("Gerar imagem")}, {QStringLiteral("ai.tool-activity.generate-image"), QStringLiteral("Desenhando: %1")}, {QStringLiteral("ai.tool-title.generate-speech"), QStringLiteral("Gerar fala")}, {QStringLiteral("ai.tool-activity.generate-speech"), QStringLiteral("Falando: %1")}, {QStringLiteral("ai.tool-title.list-voices"), QStringLiteral("Listar vozes")}, {QStringLiteral("ai.tool-title.list-skills"), QStringLiteral("Listar habilidades")}, {QStringLiteral("ai.tool-title.search-skills"), QStringLiteral("Buscar habilidades")}, {QStringLiteral("ai.tool-activity.search-skills"), QStringLiteral("Buscando por: %1")}, {QStringLiteral("ai.tool-title.read-skill"), QStringLiteral("Ler habilidade")}, {QStringLiteral("ai.tool-activity.read-skill"), QStringLiteral("Lendo %1")}, {QStringLiteral("ai.tool-title.describe-task"), QStringLiteral("Ler tarefa")}, {QStringLiteral("ai.tool-title.list-mcp-resources"), QStringLiteral("Listar recursos")}, {QStringLiteral("ai.tool-title.read-mcp-resource"), QStringLiteral("Ler recurso")}, {QStringLiteral("ai.tool-activity.read-mcp-resource"), QStringLiteral("Lendo %1")}, {QStringLiteral("ai.tool-title.list-mcp-prompts"), QStringLiteral("Listar prompts")}, {QStringLiteral("ai.tool-title.read-mcp-prompt"), QStringLiteral("Ler prompt")}, {QStringLiteral("ai.tool-activity.read-mcp-prompt"), QStringLiteral("Lendo %1")}, {QStringLiteral("ai.plugin.title"), QStringLiteral("IA")}, {QStringLiteral("ai.navigation.tasks"), QStringLiteral("Tarefas")}, {QStringLiteral("ai.tasks.title"), QStringLiteral("Tarefas de IA")}, {QStringLiteral("ai.error.title"), QStringLiteral("Tarefas de IA")}, {QStringLiteral("ai.task.tab-general"), QStringLiteral("Geral")}, {QStringLiteral("ai.task.tab-prompt"), QStringLiteral("Prompt")}, {QStringLiteral("ai.task.tab-chat"), QStringLiteral("Conversa")}, {QStringLiteral("ai.task.chat"), QStringLiteral("Abrir a conversa")}, {QStringLiteral("ai.error.stop"), QStringLiteral("Não foi possível parar a execução")}, {QStringLiteral("ai.task.tab-executions"), QStringLiteral("Execuções")}, {QStringLiteral("ai.task.tab-logs"), QStringLiteral("Logs")}, {QStringLiteral("ai.task.tab-output"), QStringLiteral("Saída")}, {QStringLiteral("ai.task.title"), QStringLiteral("Título")}, {QStringLiteral("ai.task.description"), QStringLiteral("Descrição")}, {QStringLiteral("ai.task.prompt"), QStringLiteral("Prompt")}, {QStringLiteral("ai.task.prompt-placeholder"), QStringLiteral("O que esta tarefa pede ao modelo")}, {QStringLiteral("ai.task.add"), QStringLiteral("Nova tarefa")}, {QStringLiteral("ai.task.edit"), QStringLiteral("Editar")}, {QStringLiteral("ai.task.info"), QStringLiteral("Informações da tarefa")}, {QStringLiteral("ai.task.start"), QStringLiteral("Executar")}, {QStringLiteral("ai.task.stop"), QStringLiteral("Parar")}, {QStringLiteral("ai.task.remove"), QStringLiteral("Remover tarefa")}, {QStringLiteral("ai.task.remove-message"), QStringLiteral("Remover esta tarefa permanentemente?")}, {QStringLiteral("ai.task.last-error"), QStringLiteral("Último erro: %1")}, {QStringLiteral("ai.task.scheduled"), QStringLiteral("Próxima execução %1")}, {QStringLiteral("ai.task.schedule"), QStringLiteral("Agendamento")}, {QStringLiteral("ai.task.schedule-remove"), QStringLiteral("Parar agendamento")}, {QStringLiteral("ai.task.schedule-remove-message"), QStringLiteral("Parar o agendamento desta tarefa e removê-lo?")}, {QStringLiteral("ai.task.open-workdir"), QStringLiteral("Abrir o diretório de trabalho no Editor de Código")}, {QStringLiteral("ai.task.serve-workdir"), QStringLiteral("Servir o diretório de trabalho como Web Server")}, {QStringLiteral("ai.badge.idle"), QStringLiteral("Ocioso")}, {QStringLiteral("ai.badge.scheduled"), QStringLiteral("Agendada")}, {QStringLiteral("ai.badge.stopped"), QStringLiteral("Parada")}, {QStringLiteral("ai.badge.queued"), QStringLiteral("Na fila")}, {QStringLiteral("ai.badge.running"), QStringLiteral("Executando")}, {QStringLiteral("ai.badge.succeeded"), QStringLiteral("Sucesso")}, {QStringLiteral("ai.badge.failed"), QStringLiteral("Erro")}, {QStringLiteral("ai.badge.cancelled"), QStringLiteral("Cancelado")}, {QStringLiteral("ai.status.running"), QStringLiteral("Executando")}, {QStringLiteral("ai.status.succeeded"), QStringLiteral("Sucesso")}, {QStringLiteral("ai.status.failed"), QStringLiteral("Erro")}, {QStringLiteral("ai.status.cancelled"), QStringLiteral("Cancelado")}, {QStringLiteral("ai.execution.started"), QStringLiteral("Início")}, {QStringLiteral("ai.execution.status"), QStringLiteral("Status")}, {QStringLiteral("ai.execution.tokens"), QStringLiteral("Tokens")}, {QStringLiteral("ai.execution.cost"), QStringLiteral("Custo")}, {QStringLiteral("ai.execution.token-usage"), QStringLiteral("%1 entrada, %2 saída")}, {QStringLiteral("ai.execution.finish-reason"), QStringLiteral("Motivo do término")}, {QStringLiteral("ai.execution.error"), QStringLiteral("Erro")}, {QStringLiteral("ai.execution.count"), QStringLiteral("%1 execuções registradas")}, {QStringLiteral("ai.log.timestamp"), QStringLiteral("Horário")}, {QStringLiteral("ai.log.level"), QStringLiteral("Nível")}, {QStringLiteral("ai.log-level.debug"), QStringLiteral("Depuração")}, {QStringLiteral("ai.log-level.info"), QStringLiteral("Informação")}, {QStringLiteral("ai.log-level.warning"), QStringLiteral("Aviso")}, {QStringLiteral("ai.log-level.error"), QStringLiteral("Erro")}, {QStringLiteral("ai.settings.provider"), QStringLiteral("Provedor")}, {QStringLiteral("ai.settings.model"), QStringLiteral("Modelo")}, {QStringLiteral("ai.settings.api-key"), QStringLiteral("Chave de API")}, {QStringLiteral("ai.settings.api-key-placeholder"), QStringLiteral("Chave ou {env.NOME}")}, {QStringLiteral("ai.settings.chat-font-size"), QStringLiteral("Tamanho da fonte do chat")}, {QStringLiteral("ai.settings.parallel-limit"), QStringLiteral("Execuções paralelas")}, {QStringLiteral("ai.settings.reveal-title"), QStringLiteral("Exibir chave de API")}, {QStringLiteral("ai.settings.reveal-message"), QStringLiteral("Mostrar a chave de API armazenada em texto claro?")}, {QStringLiteral("ai.settings.reveal-detail"), QStringLiteral("Qualquer pessoa que enxergue esta tela poderá ler a chave.")}, {QStringLiteral("ai.settings.reveal-action"), QStringLiteral("Exibir chave")}, {QStringLiteral("ai.provider.grok-cli"), QStringLiteral("Grok Build CLI")}, {QStringLiteral("ai.provider.claude-cli"), QStringLiteral("Claude Code CLI")}, {QStringLiteral("ai.provider.codex-cli"), QStringLiteral("Codex CLI")}, {QStringLiteral("ai.provider.antigravity-cli"), QStringLiteral("Google Antigravity CLI")}, {QStringLiteral("ai.provider.kimi-cli"), QStringLiteral("Kimi Code CLI")}, {QStringLiteral("ai.error.cli-provider-invalid"), QStringLiteral("Esta conexão não indica um agente de linha de comando")}, {QStringLiteral("ai.error.cli-workdir-required"), QStringLiteral("Um agente de linha de comando precisa da pasta de trabalho da tarefa")}, {QStringLiteral("ai.error.cli-program-missing"), QStringLiteral("O agente de linha de comando não está instalado neste sistema")}, {QStringLiteral("ai.provider.elevenlabs"), QStringLiteral("ElevenLabs")}, {QStringLiteral("ai.provider.anthropic"), QStringLiteral("Anthropic")}, {QStringLiteral("ai.provider.openai"), QStringLiteral("OpenAI")}, {QStringLiteral("ai.provider.openrouter"), QStringLiteral("OpenRouter")}, {QStringLiteral("ai.provider.deepseek"), QStringLiteral("DeepSeek")}, {QStringLiteral("ai.provider.xai"), QStringLiteral("xAI Grok")}, {QStringLiteral("ai.provider.gemini"), QStringLiteral("Google Gemini")}, {QStringLiteral("ai.provider.moonshot"), QStringLiteral("Moonshot Kimi")}, {QStringLiteral("ai.provider.groq"), QStringLiteral("Groq")}, {QStringLiteral("ai.provider.mistral"), QStringLiteral("Mistral")}, {QStringLiteral("ai.provider.together"), QStringLiteral("Together AI")}, {QStringLiteral("ai.provider.fireworks"), QStringLiteral("Fireworks AI")}, {QStringLiteral("ai.provider.deepinfra"), QStringLiteral("DeepInfra")}, {QStringLiteral("ai.provider.perplexity"), QStringLiteral("Perplexity")}, {QStringLiteral("ai.provider.cerebras"), QStringLiteral("Cerebras")}, {QStringLiteral("ai.provider.sambanova"), QStringLiteral("SambaNova")}, {QStringLiteral("ai.provider.nvidia"), QStringLiteral("NVIDIA NIM")}, {QStringLiteral("ai.provider.ollama"), QStringLiteral("Ollama")}, {QStringLiteral("ai.provider.lm-studio"), QStringLiteral("LM Studio")}, {QStringLiteral("ai.parameter.max-output-tokens"), QStringLiteral("Máximo de tokens de saída")}, {QStringLiteral("ai.parameter.temperature"), QStringLiteral("Temperatura")}, {QStringLiteral("ai.parameter.top-p"), QStringLiteral("Top P")}, {QStringLiteral("ai.parameter.frequency-penalty"), QStringLiteral("Penalidade de frequência")}, {QStringLiteral("ai.parameter.presence-penalty"), QStringLiteral("Penalidade de presença")}, {QStringLiteral("ai.parameter.reasoning-effort"), QStringLiteral("Esforço de raciocínio")}, {QStringLiteral("ai.effort.low"), QStringLiteral("Baixo")}, {QStringLiteral("ai.effort.medium"), QStringLiteral("Médio")}, {QStringLiteral("ai.effort.high"), QStringLiteral("Alto")}, {QStringLiteral("ai.column.todo"), QStringLiteral("A fazer")}, {QStringLiteral("ai.column.doing"), QStringLiteral("Fazendo")}, {QStringLiteral("ai.column.blocked"), QStringLiteral("Bloqueado")}, {QStringLiteral("ai.column.review"), QStringLiteral("Revisão")}, {QStringLiteral("ai.column.done"), QStringLiteral("Concluído")}, {QStringLiteral("ai.workspace.add"), QStringLiteral("Novo espaço de trabalho")}, {QStringLiteral("ai.workspace.rename"), QStringLiteral("Renomear espaço de trabalho")}, {QStringLiteral("ai.workspace.remove"), QStringLiteral("Remover espaço de trabalho")}, {QStringLiteral("ai.workspace.remove-message"), QStringLiteral("Remover este espaço de trabalho e todas as tarefas dele?")}, {QStringLiteral("ai.workspace.name"), QStringLiteral("Nome do espaço de trabalho")}, {QStringLiteral("ai.workspace.empty"), QStringLiteral("Crie um espaço de trabalho para organizar suas tarefas de IA")}, {QStringLiteral("ai.schedule.type"), QStringLiteral("Tipo de agendamento")}, {QStringLiteral("ai.schedule.none"), QStringLiteral("Sem agendamento")}, {QStringLiteral("ai.schedule.once"), QStringLiteral("Uma vez")}, {QStringLiteral("ai.schedule.interval"), QStringLiteral("Intervalo")}, {QStringLiteral("ai.schedule.cron"), QStringLiteral("Cron POSIX")}, {QStringLiteral("ai.schedule.minutes"), QStringLiteral(" minutos")}, {QStringLiteral("ai.validation.title"), QStringLiteral("Informe um título para a tarefa")}, {QStringLiteral("ai.validation.prompt"), QStringLiteral("Informe o prompt que o modelo recebe")}, {QStringLiteral("ai.validation.schedule-past"), QStringLiteral("O agendamento único precisa ser no futuro")}, {QStringLiteral("ai.validation.cron"), QStringLiteral("A expressão cron precisa usar os cinco campos POSIX portáveis")}, {QStringLiteral("ai.task.issue-url"), QStringLiteral("Endereço da issue")}, {QStringLiteral("ai.task.issue-url-placeholder"), QStringLiteral("Endereço da issue a que esta tarefa pertence")}, {QStringLiteral("ai.validation.issue-url"), QStringLiteral("O endereço da issue precisa ser um endereço HTTP ou HTTPS válido")}, {QStringLiteral("ai.tool.describe-task"), QStringLiteral("Ler a tarefa em que você está trabalhando, com título, descrição, endereço da issue e diretório de trabalho")}, {QStringLiteral("ai.log-kind.compacted"), QStringLiteral("Conversa compactada")}, {QStringLiteral("ai.log.compacted"), QStringLiteral("Removidos %1 turnos antigos para caber na janela de contexto do modelo")}, {QStringLiteral("ai.log.pruned"), QStringLiteral("Encurtou %1 resultados de ferramenta antigos para caber na janela de contexto do modelo")}, {QStringLiteral("ai.tool.list-skills"), QStringLiteral("Listar as habilidades disponíveis neste espaço de trabalho com as descrições")}, {QStringLiteral("ai.tool.search-skills"), QStringLiteral("Buscar as habilidades disponíveis por nome e descrição")}, {QStringLiteral("ai.tool.read-skill"), QStringLiteral("Carregar as instruções completas de uma habilidade pelo nome")}, {QStringLiteral("ai.tool.read-skill-file"), QStringLiteral("Lê um arquivo que a skill carrega junto das instruções, como a referência ou o README")}, {QStringLiteral("ai.tool-title.read-skill-file"), QStringLiteral("Ler Arquivo da Skill")}, {QStringLiteral("ai.tool-activity.read-skill-file"), QStringLiteral("Lendo %1")}, {QStringLiteral("ai.skill.none"), QStringLiteral("Este espaço de trabalho não declara habilidades")}, {QStringLiteral("ai.skill.no-match"), QStringLiteral("Nenhuma habilidade corresponde a %1")}, {QStringLiteral("ai.skill.unknown"), QStringLiteral("Não existe habilidade chamada %1")}, {QStringLiteral("ai.agent.context-file"), QStringLiteral("Instruções de %1:\n\n%2")}, {QStringLiteral("ai.agent.skills"), QStringLiteral("As habilidades a seguir estão disponíveis. Carregue uma com a ferramenta de habilidade quando ela se aplicar à tarefa.")}, {QStringLiteral("ai.agent.workdir"), QStringLiteral("Seu diretório de trabalho é %1. Todo caminho de arquivo que você usar fica dentro dele, escrito de forma relativa ou completa, e você não pode sair dele.")}, {QStringLiteral("ai.agent.no-workdir"), QStringLiteral("Esta tarefa não declara diretório de trabalho, então as ferramentas de arquivo não estão disponíveis para você.")}, {QStringLiteral("ai.tool.generate-speech"), QStringLiteral("Transformar texto em fala e guardar como artefato da execução")}, {QStringLiteral("ai.error.tool-speech-unconfigured"), QStringLiteral("A geração de fala precisa de texto e de um provedor de fala configurado")}, {QStringLiteral("ai.error.tool-speech-empty"), QStringLiteral("O provedor de fala não devolveu áudio")}, {QStringLiteral("ai.settings.speech"), QStringLiteral("Provedor de fala")}, {QStringLiteral("ai.settings.speech-voice"), QStringLiteral("Voz")}, {QStringLiteral("ai.settings.speech-key"), QStringLiteral("Chave de API")}, {QStringLiteral("ai.tool.generate-image"), QStringLiteral("Gerar uma imagem a partir de uma descrição e guardá-la como artefato da execução")}, {QStringLiteral("ai.error.tool-image-unconfigured"), QStringLiteral("A geração de imagem precisa de um prompt e de um endereço de provedor configurado")}, {QStringLiteral("ai.error.tool-image-empty"), QStringLiteral("O provedor não devolveu dados de imagem")}, {QStringLiteral("ai.tool.fetch-url"), QStringLiteral("Buscar uma página web e devolver o texto legível")}, {QStringLiteral("ai.error.tool-url"), QStringLiteral("O endereço %1 não é um endereço HTTP ou HTTPS válido")}, {QStringLiteral("ai.tool.read-file"), QStringLiteral("Ler um arquivo de texto dentro do diretório de trabalho da tarefa")}, {QStringLiteral("ai.tool.write-file"), QStringLiteral("Escrever um arquivo de texto dentro do diretório de trabalho da tarefa")}, {QStringLiteral("ai.tool.edit-file"), QStringLiteral("Substituir um trecho exato de um arquivo de texto dentro do diretório de trabalho da tarefa")}, {QStringLiteral("ai.tool.read-image"), QStringLiteral("Olhar um arquivo de imagem dentro do diretório de trabalho da tarefa")}, {QStringLiteral("ai.error.tool-image-unsupported"), QStringLiteral("O modelo %1 não aceita imagens, então não pode olhar uma")}, {QStringLiteral("ai.error.tool-image-format"), QStringLiteral("O arquivo %1 não é uma imagem PNG, JPEG, WebP ou GIF")}, {QStringLiteral("ai.error.tool-image-size"), QStringLiteral("A imagem %1 está vazia ou é maior do que a ferramenta aceita")}, {QStringLiteral("ai.error.tool-edit-absent"), QStringLiteral("O arquivo %1 não contém exatamente esse texto. Leia o arquivo de novo e copie o trecho exatamente como está escrito lá.")}, {QStringLiteral("ai.error.tool-edit-ambiguous"), QStringLiteral("Esse texto aparece %1 vezes, então indique um trecho maior ou peça para substituir todas as ocorrências")}, {QStringLiteral("ai.error.tool-not-text"), QStringLiteral("%1 não é texto UTF-8, então não pode ser lido nem editado sem reescrever o que ele contém")}, {QStringLiteral("ai.tool.list-directory"), QStringLiteral("Listar as entradas de um diretório dentro do diretório de trabalho da tarefa")}, {QStringLiteral("ai.error.tool-argument-value"), QStringLiteral("A ferramenta %1 recebeu um valor inutilizável para o argumento %2")}, {QStringLiteral("ai.error.tool-argument-missing"), QStringLiteral("A ferramenta %1 exige o argumento %2")}, {QStringLiteral("ai.error.tool-arguments-unreadable"), QStringLiteral("Os argumentos de %1 não puderam ser lidos como JSON. Eles chegaram assim: %2. Chame a ferramenta de novo com argumentos JSON válidos.")}, {QStringLiteral("ai.error.tool-argument-type"), QStringLiteral("A ferramenta %1 espera que o argumento %2 seja do tipo %3")}, {QStringLiteral("ai.error.tool-path-missing"), QStringLiteral("O caminho %1 não existe")}, {QStringLiteral("ai.error.destination-unavailable"), QStringLiteral("O plugin que abre essa pasta não está disponível")}, {QStringLiteral("ai.error.tool-workdir-none"), QStringLiteral("Esta tarefa não declara diretório de trabalho, então não tem acesso a arquivos")}, {QStringLiteral("ai.error.tool-workdir-unavailable"), QStringLiteral("O diretório de trabalho %1 está indisponível")}, {QStringLiteral("ai.error.tool-path-outside"), QStringLiteral("O caminho %1 está fora do diretório de trabalho %2")}, {QStringLiteral("ai.error.tool-repeated"), QStringLiteral("O agente repetiu a ferramenta %1 sem progresso")}, {QStringLiteral("ai.stop-reason.answered"), QStringLiteral("Respondeu")}, {QStringLiteral("ai.stop-reason.iteration-limit"), QStringLiteral("Parou no limite de iterações")}, {QStringLiteral("ai.stop-reason.output-budget"), QStringLiteral("Parou no limite da resposta")}, {QStringLiteral("ai.stop-reason.tool-repetition"), QStringLiteral("Parou após repetir uma ferramenta")}, {QStringLiteral("ai.stop-reason.cancelled"), QStringLiteral("Parado por quem lê")}, {QStringLiteral("ai.stop-reason.failed"), QStringLiteral("Falhou")}, {QStringLiteral("ai.agent.final-turn"), QStringLiteral("Este é o último turno desta execução. Responda agora com o que você já tem e não chame outra ferramenta.")}, {QStringLiteral("ai.tool.picture-not-sent"), QStringLiteral("A imagem deste resultado não foi enviada porque o modelo selecionado não lê imagens.")}, {QStringLiteral("ai.error.tool-repeated-reason"), QStringLiteral("O agente repetiu a ferramenta %1 sem progresso, e ela respondia: %2")}, {QStringLiteral("ai.error.tool-unknown"), QStringLiteral("O modelo pediu uma ferramenta desconhecida %1")}, {QStringLiteral("ai.error.tool-deadline"), QStringLiteral("A ferramenta %1 não respondeu a tempo")}, {QStringLiteral("ai.error.tool-cancelled"), QStringLiteral("A ferramenta foi interrompida junto com a tarefa")}, {QStringLiteral("ai.error.output-truncated"), QStringLiteral("O modelo parou no limite de %1 tokens de saída, então a resposta está incompleta")}, {QStringLiteral("ai.settings.execution"), QStringLiteral("Execução")}, {QStringLiteral("ai.conversation.back"), QStringLiteral("Fechar a conversa")}, {QStringLiteral("ai.conversation.send"), QStringLiteral("Enviar")}, {QStringLiteral("ai.conversation.placeholder"), QStringLiteral("Escreva uma mensagem, Shift e Enter para nova linha")}, {QStringLiteral("ai.conversation.empty"), QStringLiteral("Envie uma mensagem ou execute a tarefa para começar esta conversa")}, {QStringLiteral("ai.conversation.reset"), QStringLiteral("Zerar Conversa")}, {QStringLiteral("ai.conversation.reset-title"), QStringLiteral("Zerar Conversa")}, {QStringLiteral("ai.conversation.reset-message"), QStringLiteral("Limpar esta conversa?")}, {QStringLiteral("ai.conversation.reset-detail"), QStringLiteral("As mensagens e as execuções que esta tarefa registrou são removidas em definitivo.")}, {QStringLiteral("ai.settings.group-connections"), QStringLiteral("IA Conexões")}, {QStringLiteral("ai.settings.group-providers"), QStringLiteral("IA Provedores")}, {QStringLiteral("ai.settings.group-agents"), QStringLiteral("IA Agentes")}, {QStringLiteral("ai.settings.group-tools"), QStringLiteral("IA Ferramentas")}, {QStringLiteral("ai.settings.group-general"), QStringLiteral("IA Geral")}, {QStringLiteral("ai.settings.agents"), QStringLiteral("Agentes")}, {QStringLiteral("ai.agent.add"), QStringLiteral("Novo Agente")}, {QStringLiteral("ai.agent.edit"), QStringLiteral("Editar Agente")}, {QStringLiteral("ai.agent.remove"), QStringLiteral("Remover Agente")}, {QStringLiteral("ai.agent.remove-title"), QStringLiteral("Remover Agente")}, {QStringLiteral("ai.agent.remove-message"), QStringLiteral("Remover este agente?")}, {QStringLiteral("ai.agent.remove-detail"), QStringLiteral("%1 tarefas usam este agente e vão parar com erro até outro ser escolhido.")}, {QStringLiteral("ai.agent.identifier"), QStringLiteral("Identificador")}, {QStringLiteral("ai.agent.identifier-hint"), QStringLiteral("Deixe vazio e ele é escrito a partir do nome.")}, {QStringLiteral("ai.agent.identifier-placeholder"), QStringLiteral("Letras minúsculas, números e hífens")}, {QStringLiteral("ai.agent.name"), QStringLiteral("Nome")}, {QStringLiteral("ai.agent.description"), QStringLiteral("Descrição")}, {QStringLiteral("ai.agent.description-placeholder"), QStringLiteral("No que este agente é especialista")}, {QStringLiteral("ai.agent.connection"), QStringLiteral("Conexão de modelo")}, {QStringLiteral("ai.agent.maximum-iterations"), QStringLiteral("Máximo de iterações")}, {QStringLiteral("ai.agent.system-prompt"), QStringLiteral("Prompt de sistema")}, {QStringLiteral("ai.agent.system-prompt-placeholder"), QStringLiteral("As instruções que este agente sempre recebe")}, {QStringLiteral("ai.agent.insert-template"), QStringLiteral("Inserir modelo")}, {QStringLiteral("ai.agent.show-tags"), QStringLiteral("Tags disponíveis")}, {QStringLiteral("ai.agent.empty"), QStringLiteral("Nenhum agente configurado")}, {QStringLiteral("ai.task.agent"), QStringLiteral("Agente")}, {QStringLiteral("ai.validation.agent-identifier"), QStringLiteral("O identificador deve usar letras minúsculas, números e hífens")}, {QStringLiteral("ai.validation.agent-duplicate"), QStringLiteral("Outro agente já usa este identificador")}, {QStringLiteral("ai.validation.agent-name"), QStringLiteral("Informe um nome para este agente")}, {QStringLiteral("ai.validation.agent-prompt"), QStringLiteral("Informe o prompt de sistema que este agente sempre recebe")}, {QStringLiteral("ai.validation.agent-tag"), QStringLiteral("O prompt de sistema traz uma tag que ninguém declara: %1")}, {QStringLiteral("ai.validation.agent-missing"), QStringLiteral("Crie um agente nas configurações de IA e escolha-o aqui")}, {QStringLiteral("ai.error.agent-removed"), QStringLiteral("O agente %1 foi removido, então esta tarefa não tem quem a execute")}, {QStringLiteral("ai.error.agent-save"), QStringLiteral("Os agentes não puderam ser salvos")}, {QStringLiteral("ai.error.connection-in-use"), QStringLiteral("O agente %1 usa esta conexão. Aponte esse agente para outra conexão antes de remover esta.")}, {QStringLiteral("ai.error.conversation-save"), QStringLiteral("A conversa não pôde ser registrada")}, {QStringLiteral("ai.tag.system-prompt-data"), QStringLiteral("O diretório de trabalho, o ambiente, os arquivos de contexto e as skills da tarefa")}, {QStringLiteral("ai.tag.agent-name"), QStringLiteral("O nome deste agente")}, {QStringLiteral("ai.tag.agent-description"), QStringLiteral("A descrição deste agente")}, {QStringLiteral("ai.tag.task-title"), QStringLiteral("O título da tarefa")}, {QStringLiteral("ai.tag.task-description"), QStringLiteral("A descrição da tarefa")}, {QStringLiteral("ai.tag.task-prompt"), QStringLiteral("O prompt da tarefa")}, {QStringLiteral("ai.tag.task-workdir"), QStringLiteral("O diretório de trabalho da tarefa")}, {QStringLiteral("ai.tag.task-issue-url"), QStringLiteral("O endereço da issue da tarefa")}, {QStringLiteral("ai.tag.date-time"), QStringLiteral("A data e a hora locais")}, {QStringLiteral("ai.tag.date-time-utc"), QStringLiteral("A data e a hora em UTC")}, {QStringLiteral("ai.tag.time-zone"), QStringLiteral("O fuso horário desta máquina")}, {QStringLiteral("ai.tag.locale"), QStringLiteral("O locale desta máquina")}, {QStringLiteral("ai.tag.language"), QStringLiteral("O idioma desta máquina")}, {QStringLiteral("ai.tag.operating-system"), QStringLiteral("O sistema operacional desta máquina")}, {QStringLiteral("ai.tag.user-name"), QStringLiteral("O usuário que esta máquina informa")}, {QStringLiteral("ai.tag.home-directory"), QStringLiteral("O diretório pessoal do usuário")}, {QStringLiteral("ai.tag.tools"), QStringLiteral("Os nomes das ferramentas que o agente pode chamar")}, {QStringLiteral("ai.tag.model"), QStringLiteral("O provedor e o modelo com que esta execução fala")}, {QStringLiteral("ai.tag.model-traits"), QStringLiteral("O que o modelo selecionado declara que sabe fazer")}, {QStringLiteral("ai.tag.vision"), QStringLiteral("Se o modelo selecionado lê uma imagem")}, {QStringLiteral("ai.tag.search"), QStringLiteral("Se um serviço de busca na web está configurado")}, {QStringLiteral("ai.tag.speech"), QStringLiteral("Se um serviço de fala está configurado")}, {QStringLiteral("ai.tag.servers"), QStringLiteral("Os servidores do Model Context Protocol conectados agora")}, {QStringLiteral("ai.tag.context-window"), QStringLiteral("Quantos tokens a janela do modelo selecionado comporta")}, {QStringLiteral("ai.tag.output-budget"), QStringLiteral("Quantos tokens a resposta de um turno pode usar")}, {QStringLiteral("ai.capability.vision-yes"), QStringLiteral("Você lê imagens. Use read_image em uma figura que importe para a tarefa.")}, {QStringLiteral("ai.capability.vision-no"), QStringLiteral("Você não lê imagens. Não peça uma, trabalhe pelo texto.")}, {QStringLiteral("ai.capability.search-yes"), QStringLiteral("Você pode buscar na web através de %1.")}, {QStringLiteral("ai.capability.search-no"), QStringLiteral("Nenhum serviço de busca está configurado, então web_search vai recusar.")}, {QStringLiteral("ai.capability.speech-yes"), QStringLiteral("Você pode gerar fala através de %1.")}, {QStringLiteral("ai.capability.speech-no"), QStringLiteral("Nenhum serviço de fala está configurado, então generate_speech vai recusar.")}, {QStringLiteral("ai.capability.servers"), QStringLiteral("Estes servidores estão conectados e as ferramentas deles são suas: %1.")}, {QStringLiteral("ai.capability.servers-none"), QStringLiteral("Nenhum servidor está conectado, então só as ferramentas deste aplicativo estão disponíveis.")}, {QStringLiteral("ai.tag.skills"), QStringLiteral("As skills que o diretório de trabalho declara")}, {QStringLiteral("ai.tag.context-files"), QStringLiteral("Os arquivos de contexto publicados do diretório de trabalho")}, {QStringLiteral("ai.settings.rate-limits"), QStringLiteral("Limites de requisição")}, {QStringLiteral("ai.settings.provider-selection"), QStringLiteral("Provedor")}, {QStringLiteral("ai.settings.provider-scope"), QStringLiteral("Configurações de")}, {QStringLiteral("ai.settings.rate-limit-interval"), QStringLiteral("Intervalo entre requisições em milissegundos")}, {QStringLiteral("ai.settings.rate-limit-per-minute"), QStringLiteral("Máximo de requisições por minuto")}, {QStringLiteral("ai.settings.rate-limit-concurrent"), QStringLiteral("Máximo de requisições ao mesmo tempo")}, {QStringLiteral("ai.phase.throttled"), QStringLiteral("Aguardando o provedor")}, {QStringLiteral("ai.log-kind.throttled"), QStringLiteral("Aguardando antes da requisição")}, {QStringLiteral("ai.log.rate-limit-delay"), QStringLiteral("Aguardando %1 ms antes de chamar o provedor")}, {QStringLiteral("ai.log.retry-delay"), QStringLiteral("O serviço recusou a requisição, tentando de novo em %1 ms")}, {QStringLiteral("ai.settings.maximum-iterations"), QStringLiteral("Máximo de iterações do agente")}, {QStringLiteral("ai.settings.command-timeout"), QStringLiteral("Tempo limite do comando em segundos")}, {QStringLiteral("ai.task.execution-kind"), QStringLiteral("Execução")}, {QStringLiteral("ai.task.kind-agent"), QStringLiteral("Agente de IA")}, {QStringLiteral("ai.task.kind-command"), QStringLiteral("Comando")}, {QStringLiteral("ai.task.workdir"), QStringLiteral("Diretório de trabalho")}, {QStringLiteral("ai.task.choose-workdir"), QStringLiteral("Escolher diretório de trabalho")}, {QStringLiteral("ai.task.command"), QStringLiteral("Comando")}, {QStringLiteral("ai.task.command-timeout"), QStringLiteral("Tempo limite em segundos")}, {QStringLiteral("ai.task.unlimited-hint"), QStringLiteral("Zero significa ilimitado")}, {QStringLiteral("ai.validation.command"), QStringLiteral("Informe o comando que esta tarefa executa")}, {QStringLiteral("ai.validation.workdir"), QStringLiteral("Escolha um diretório de trabalho absoluto e existente")}, {QStringLiteral("ai.error.exit-code"), QStringLiteral("O comando terminou com código %1")}, {QStringLiteral("ai.error.command-timeout"), QStringLiteral("O comando excedeu seu limite de tempo")}, {QStringLiteral("ai.error.command-output-too-large"), QStringLiteral("O comando produziu mais saída do que o permitido")}, {QStringLiteral("ai.error.command-workdir-invalid"), QStringLiteral("O diretório de trabalho do comando está indisponível")}, {QStringLiteral("ai.error.command-start-failed"), QStringLiteral("O comando não pôde ser iniciado")}, {QStringLiteral("ai.error.command-crashed"), QStringLiteral("O comando terminou de forma anormal")}, {QStringLiteral("ai.error.tool-answer-truncated"), QStringLiteral("O serviço respondeu com mais do que o tamanho permitido, então nenhum arquivo foi gravado")}, {QStringLiteral("ai.error.endpoint-unavailable"), QStringLiteral("O provedor selecionado não responde esse tipo de requisição")}, {QStringLiteral("ai.phase.calling-tool"), QStringLiteral("Chamando %1")}, {QStringLiteral("ai.phase.tool-count"), QStringLiteral("%1 ferramentas")}, {QStringLiteral("ai.phase.compacting"), QStringLiteral("Resumindo a conversa anterior")}, {QStringLiteral("ai.phase.running"), QStringLiteral("Executando o comando")}, {QStringLiteral("ai.log-kind.tool-called"), QStringLiteral("Ferramenta chamada")}, {QStringLiteral("ai.log-kind.tool-returned"), QStringLiteral("Ferramenta retornou")}, {QStringLiteral("ai.log-kind.iteration"), QStringLiteral("Iteração do agente")}, {QStringLiteral("ai.log.event"), QStringLiteral("Evento")}, {QStringLiteral("ai.log.detail"), QStringLiteral("Detalhe")}, {QStringLiteral("ai.log.usage"), QStringLiteral("%1 tokens de entrada, %2 de saída, terminou como %3")}, {QStringLiteral("ai.log-kind.started"), QStringLiteral("Execução iniciada")}, {QStringLiteral("ai.log-kind.request-sent"), QStringLiteral("Requisição enviada ao modelo")}, {QStringLiteral("ai.log-kind.first-token"), QStringLiteral("Primeiro conteúdo recebido")}, {QStringLiteral("ai.log-kind.response-received"), QStringLiteral("Resposta completa recebida")}, {QStringLiteral("ai.log-kind.usage-reported"), QStringLiteral("Uso de tokens informado")}, {QStringLiteral("ai.log-kind.succeeded"), QStringLiteral("Execução concluída")}, {QStringLiteral("ai.log-kind.failed"), QStringLiteral("Execução falhou")}, {QStringLiteral("ai.log-kind.cancelled"), QStringLiteral("Execução cancelada")}, {QStringLiteral("ai.phase.idle"), QStringLiteral("Ocioso")}, {QStringLiteral("ai.phase.queued"), QStringLiteral("Aguardando capacidade")}, {QStringLiteral("ai.phase.sending"), QStringLiteral("Aguardando o modelo")}, {QStringLiteral("ai.phase.streaming"), QStringLiteral("Recebendo a resposta")}, {QStringLiteral("ai.validation.api-key"), QStringLiteral("Informe a chave de API que este provedor exige")}, {QStringLiteral("ai.validation.model"), QStringLiteral("Escolha o modelo que este provedor deve executar")}, {QStringLiteral("ai.validation.output-budget-unknown"), QStringLiteral("O catálogo não declara o modelo %1, então informe o máximo de tokens de saída que ele aceita em vez de deixar zero")}, {QStringLiteral("ai.validation.output-budget-whole-window"), QStringLiteral("O modelo %1 declara a janela inteira como resposta máxima, então informe os tokens a deixar para essa resposta em vez de zero")}, {QStringLiteral("ai.output.none"), QStringLiteral("Selecione uma execução para ver a saída")}, {QStringLiteral("ai.output.running"), QStringLiteral("A execução ainda está em andamento")}, {QStringLiteral("ai.output.failed"), QStringLiteral("A execução falhou antes de o modelo devolver qualquer texto")}, {QStringLiteral("ai.output.cancelled"), QStringLiteral("A execução foi cancelada antes de o modelo devolver qualquer texto")}, {QStringLiteral("ai.output.empty"), QStringLiteral("O modelo não devolveu texto")}, {QStringLiteral("ai.agent.environment"), QStringLiteral("Esta é a máquina e o momento em que você está executando. Use isso sempre que a tarefa depender da data atual, do idioma ou dos caminhos deste sistema.")}, {QStringLiteral("ai.log.open-payload"), QStringLiteral("Ver conteúdo")}, {QStringLiteral("ai.task.schedule-done"), QStringLiteral("Executada em %1")}, {QStringLiteral("ai.tool.create-directory"), QStringLiteral("Criar um diretório dentro do diretório de trabalho da tarefa")}, {QStringLiteral("ai.tool.move-path"), QStringLiteral("Mover ou renomear um arquivo ou diretório dentro do diretório de trabalho da tarefa")}, {QStringLiteral("ai.tool.copy-file"), QStringLiteral("Copiar um arquivo dentro do diretório de trabalho da tarefa")}, {QStringLiteral("ai.tool.remove-path"), QStringLiteral("Remover um arquivo ou um diretório vazio dentro do diretório de trabalho da tarefa")}, {QStringLiteral("ai.error.tool-remove-not-empty"), QStringLiteral("O diretório %1 não está vazio, então peça de novo com recursive verdadeiro para remover o que está dentro dele")}, {QStringLiteral("ai.tool.describe-path"), QStringLiteral("Informar tipo, tamanho, data de modificação e permissões de um caminho")}, {QStringLiteral("ai.tool.search-files"), QStringLiteral("Buscar arquivos por padrão de nome e, opcionalmente, pelo texto que contêm")}, {QStringLiteral("ai.tool.run-command"), QStringLiteral("Executar um comando dentro do diretório de trabalho da tarefa e retornar a saída")}, {QStringLiteral("ai.tool.list-voices"), QStringLiteral("Listar as vozes que o serviço de fala configurado publica")}, {QStringLiteral("ai.tool.command-no-output"), QStringLiteral("O comando não produziu saída")}, {QStringLiteral("ai.error.tool-no-match"), QStringLiteral("Nenhum item corresponde à busca")}, {QStringLiteral("ai.error.tool-sandbox-missing"), QStringLiteral("Esta tarefa não declara diretório de trabalho, então não pode executar comandos")}, {QStringLiteral("ai.error.tool-voices-empty"), QStringLiteral("O serviço de fala não publicou nenhuma voz")}, {QStringLiteral("ai.settings.speech-provider"), QStringLiteral("Serviço")}, {QStringLiteral("ai.settings.speech-voice-placeholder"), QStringLiteral("Identificador da voz que vai falar")}, {QStringLiteral("ai.settings.search-instance"), QStringLiteral("Endereço da instância")}, {QStringLiteral("ai.settings.search-instance-placeholder"), QStringLiteral("Endereço da sua instância SearXNG")}, {QStringLiteral("ai.tool.list-mcp-resources"), QStringLiteral("Listar os recursos que cada servidor conectado publica")}, {QStringLiteral("ai.tool.read-mcp-resource"), QStringLiteral("Ler um recurso publicado por um servidor conectado")}, {QStringLiteral("ai.tool.list-mcp-prompts"), QStringLiteral("Listar os prompts que cada servidor conectado publica")}, {QStringLiteral("ai.tool.read-mcp-prompt"), QStringLiteral("Carregar um prompt publicado por um servidor conectado")}, {QStringLiteral("ai.error.mcp-none"), QStringLiteral("Nenhum servidor está conectado")}, {QStringLiteral("ai.settings.refresh-models"), QStringLiteral("Atualizar os modelos que este provedor publica")}, {QStringLiteral("ai.validation.no-model-published"), QStringLiteral("O provedor não publicou nenhum modelo para esta credencial")}, {QStringLiteral("ai.agent.summarize"), QStringLiteral("Resuma a conversa abaixo nos fatos, decisões e resultados que o agente ainda precisa. Responda apenas com o resumo.")}, {QStringLiteral("ai.agent.summary"), QStringLiteral("Resumo da parte anterior desta execução:\n\n%1")}, {QStringLiteral("ai.tool.web-search"), QStringLiteral("Buscar na web e retornar título, endereço e resumo de cada resultado")}, {QStringLiteral("ai.error.tool-search-unconfigured"), QStringLiteral("A busca web precisa de um termo e de um provedor de busca configurado")}, {QStringLiteral("ai.search.no-result"), QStringLiteral("A busca não retornou resultado")}, {QStringLiteral("ai.settings.search"), QStringLiteral("Busca web")}, {QStringLiteral("ai.settings.search-provider"), QStringLiteral("Serviço")}, {QStringLiteral("ai.settings.search-key"), QStringLiteral("Chave de API")}, {QStringLiteral("ai.search.brave"), QStringLiteral("Brave Search")}, {QStringLiteral("ai.search.tavily"), QStringLiteral("Tavily")}, {QStringLiteral("ai.search.searxng"), QStringLiteral("SearXNG")}, {QStringLiteral("ai.settings.mcp"), QStringLiteral("Servidores MCP")}, {QStringLiteral("ai.mcp.add"), QStringLiteral("Novo servidor")}, {QStringLiteral("ai.mcp.edit"), QStringLiteral("Editar servidor")}, {QStringLiteral("ai.mcp.remove"), QStringLiteral("Remover servidor")}, {QStringLiteral("ai.mcp.remove-title"), QStringLiteral("Remover servidor MCP")}, {QStringLiteral("ai.mcp.remove-message"), QStringLiteral("Remover este servidor e as ferramentas que ele publica?")}, {QStringLiteral("ai.mcp.identifier"), QStringLiteral("Identificador")}, {QStringLiteral("ai.mcp.identifier-placeholder"), QStringLiteral("Letras minúsculas, números e hifens")}, {QStringLiteral("ai.mcp.transport"), QStringLiteral("Transporte")}, {QStringLiteral("ai.mcp.transport-stdio"), QStringLiteral("Processo local")}, {QStringLiteral("ai.mcp.transport-http"), QStringLiteral("HTTP")}, {QStringLiteral("ai.mcp.command"), QStringLiteral("Comando")}, {QStringLiteral("ai.mcp.arguments"), QStringLiteral("Argumentos")}, {QStringLiteral("ai.mcp.workdir"), QStringLiteral("Diretório de trabalho")}, {QStringLiteral("ai.mcp.address"), QStringLiteral("Endereço")}, {QStringLiteral("ai.mcp.api-key"), QStringLiteral("Chave de API")}, {QStringLiteral("ai.mcp.roots"), QStringLiteral("Raízes")}, {QStringLiteral("ai.mcp.roots-placeholder"), QStringLiteral("Um diretório absoluto por linha")}, {QStringLiteral("ai.mcp.sampling"), QStringLiteral("Permitir que o servidor consulte o modelo")}, {QStringLiteral("ai.mcp.sampling-tokens"), QStringLiteral("Máximo de tokens por consulta do servidor")}, {QStringLiteral("ai.mcp.target"), QStringLiteral("Destino")}, {QStringLiteral("ai.mcp.tools"), QStringLiteral("Ferramentas")}, {QStringLiteral("ai.mcp.empty"), QStringLiteral("Nenhum servidor cadastrado")}, {QStringLiteral("ai.validation.mcp-identifier"), QStringLiteral("O identificador precisa usar letras minúsculas, números e hifens")}, {QStringLiteral("ai.validation.mcp-duplicate"), QStringLiteral("Outro servidor já usa este identificador")}, {QStringLiteral("ai.validation.mcp-command"), QStringLiteral("Informe o comando que inicia o servidor")}, {QStringLiteral("ai.validation.mcp-address"), QStringLiteral("Informe um endereço HTTP ou HTTPS válido para este servidor")}, {QStringLiteral("ai.error.mcp-save"), QStringLiteral("Não foi possível salvar os servidores MCP")}, {QStringLiteral("ai.error.mcp-unavailable"), QStringLiteral("O servidor que publica a ferramenta %1 está indisponível")}, {QStringLiteral("ai.settings.connections"), QStringLiteral("Conexões de modelo")}, {QStringLiteral("ai.settings.default-connection"), QStringLiteral("Conexão padrão")}, {QStringLiteral("ai.connection.add"), QStringLiteral("Nova conexão")}, {QStringLiteral("ai.connection.edit"), QStringLiteral("Editar conexão")}, {QStringLiteral("ai.connection.remove"), QStringLiteral("Remover conexão")}, {QStringLiteral("ai.connection.remove-title"), QStringLiteral("Remover conexão de modelo")}, {QStringLiteral("ai.connection.remove-message"), QStringLiteral("Remover esta conexão? As tarefas que rodam nela vão parar até apontarem para outra.")}, {QStringLiteral("ai.connection.display-name"), QStringLiteral("Nome de exibição")}, {QStringLiteral("ai.connection.display-name-hint"), QStringLiteral("Deixe vazio e o provedor e o modelo são mostrados.")}, {QStringLiteral("ai.connection.display-name-placeholder"), QStringLiteral("Nome mostrado quando uma tarefa escolhe esta conexão")}, {QStringLiteral("ai.connection.address"), QStringLiteral("Endereço do serviço")}, {QStringLiteral("ai.connection.address-placeholder"), QStringLiteral("Endereço do seu serviço local")}, {QStringLiteral("ai.connection.empty"), QStringLiteral("Nenhuma conexão configurada")}, {QStringLiteral("ai.connection.extra-parameters"), QStringLiteral("Parâmetros extras")}, {QStringLiteral("ai.connection.extra-parameters-description"), QStringLiteral("Um objeto JSON mesclado sobre os parâmetros acima no momento do envio. Use para qualquer coisa que o serviço aceite e não esteja listada aqui, escreva a chave com ponto para alcançar um campo aninhado e use null para remover um campo que o serviço não pode receber.")}, {QStringLiteral("ai.connection.extra-parameters-placeholder"), QStringLiteral("{\"seed\": 42, \"top_k\": 40}")}, {QStringLiteral("ai.validation.connection-duplicate"), QStringLiteral("Este par de provedor e modelo já está configurado")}, {QStringLiteral("ai.validation.connection-address"), QStringLiteral("Informe um endereço HTTP ou HTTPS válido para este serviço")}, {QStringLiteral("ai.validation.connection-missing"), QStringLiteral("Configure uma conexão de modelo nas configurações de IA e escolha-a aqui")}, {QStringLiteral("ai.validation.extra-parameters-object"), QStringLiteral("Os parâmetros extras precisam ser um objeto JSON")}, {QStringLiteral("ai.validation.extra-parameters-syntax"), QStringLiteral("Os parâmetros extras não são JSON válido: %1 na posição %2")}, {QStringLiteral("ai.validation.extra-parameters-key"), QStringLiteral("O nome de um parâmetro extra não pode ser vazio")}, {QStringLiteral("ai.error.connection-save"), QStringLiteral("Não foi possível salvar as conexões de modelo")}, {QStringLiteral("ai.error.connection-missing"), QStringLiteral("A conexão %1 não está configurada, então o agente desta tarefa não pode ser executado")}, {QStringLiteral("ai.error.service-save"), QStringLiteral("Não foi possível salvar as configurações do serviço")}, {QStringLiteral("ai.error.settings-save"), QStringLiteral("Não foi possível salvar as configurações da IA")}, {QStringLiteral("ai.error.workspace-save"), QStringLiteral("Não foi possível salvar a área de trabalho")}, {QStringLiteral("ai.error.task-run"), QStringLiteral("Não foi possível iniciar ou parar a tarefa")}, {QStringLiteral("ai.error.execution-save"), QStringLiteral("Não foi possível registrar a execução")}, {QStringLiteral("ai.error.log-save"), QStringLiteral("Não foi possível registrar o log da execução")}, {QStringLiteral("ai.error.task-save"), QStringLiteral("Não foi possível salvar a tarefa")}, {QStringLiteral("ai.error.schedule-save"), QStringLiteral("Não foi possível avançar o agendamento")}, {QStringLiteral("ai.error.server-failed"), QStringLiteral("Um servidor Model Context Protocol relatou uma falha")}};
    }

    static TranslationCatalog catalog() {
        return {{QStringLiteral("en"), english()}, {QStringLiteral("pt"), portuguese()}};
    }
};

} // namespace workpane::plugins::ai::translations
